# وضعیت پروژه و ادامه‌ی کار — ثبت ۲۰۲۶-۰۸-۰۸ (به‌روز ۲۰۲۶-۰۸-۱۱)

## ۰-الف. به‌روزرسانی ۲۰۲۶-۰۸-۱۱ — «هوشمندی v4» اجرا شد (ران خودمختار ۸ساعته)

- هر شش ستون v4 پیاده و سبز؛ شواهد کامل: `docs/DESIGN_intelligence_v4.md` §8
  و بخش v4 در `RIG_GUIDE.md`. اعداد دروازه: GATE LOOP OK (۲۶ سوئیت)؛ بازپخش
  روی همان کورپوس ۱٫۱۰۷ در برابر ۱٫۰۸۵؛ کالیبراتور ۰٫۹۵۹←۱٫۰۴۴؛ ردیف ریگ
  extreme با preventedFreezes=58 و صفر قطعی در ۴۵۸ ثانیه.
- امروز هر ۸ پروفایل SLA روی ۱۱ پرومکس با بیلد v4 سبز شد (loss60 دو بار،
  extreme دو بار — پشته دیگر زیر extreme هم نمی‌میرد). کورپوس فدرال: ۳۸ ران
  (۱۰ ران TREND-دار)؛ هر ران h2_run از این پس خودکار ثبت می‌شود.
- یافته‌ی قرمز جدید (خارج از دامنه‌ی v4، چیپ جلسه‌ی جدا ساخته شد): خط عکسِ
  staged روی دستگاه صفر بایت ack می‌گیرد (۳ ران، ۲ پروفایل، یک امضا؛ متن/ویس
  سالم) — رگرسیون تغییرات ۰۸-۰۹/۱۰ خط عکس؛ ردیف‌ها صادقانه FAIL ثبت شدند.
- بازِ ثبت‌شده: sentinelLeadMs (هنوز فریزی رخ نداده که عدد بدهد)، کورپوس ۱۰۰+،
  آموزش TrendTuner (منتظر ردیف‌های TREND-دار بیشتر)، پراب واقعی resolver +
  ماندگاری مغزها در دو لانچ روی گوشی.

این سند تنها یک کار دارد: هر کس نشست بعد را باز کرد، بدون خواندن تاریخچه
بداند دقیقاً کجا ایستاده‌ایم و قدم بعدی چیست.

## ۰. به‌روزرسانی ۲۰۲۶-۰۸-۰۹ — توپولوژی USB و ادامه از اینجا

- راهنمای عملیاتی زنده و کامل‌تر: `RIG_GUIDE.md` در ریشه — بخش «ازسرگیری
  فوری» آن نقطه‌ی شروع هر چت جدید است؛ این سند برای تاریخچه و اعداد است.
- تشخیص قطعی شب ۲۲قرعه‌ای تأیید و بسته شد: قاتل، رادیوی AP مک بود.
  راه‌حل نصب‌شده: داده‌ی تست فقط از کابل USB (پایه ~۰.۹ms و ۰٪)؛
  دروازه‌ی مکانیکی فقط-سیم داخل `tools/t2/h2_run.sh` (رد رادیو + رد
  مسیر ناسالم، تست‌شده‌ی زنده). دیگر هیچ قرعه‌ای قربانی رادیو نمی‌شود.
- دستگاه تست فعلی: آیفون ۱۴ پرومکس `00008120-0010795A3613C01E`؛ ذخیره:
  ۱۱ پرومکس (بدون سیم — در الگوی USB کاملاً کافی). سویچ = یک فرمان
  کشف خودکار `tools/t2/usb_peer.sh` (در RIG_GUIDE ثبت است).
- خطوط خودکار ماندگار شدند: `tools/t2/pipelines/` (تک‌تیر ویدیو →
  مهر رکورد → صوت آیتم ۸ → پیام‌رسانی؛ توقف در اولین سبز؛ شات‌های
  صفر-بسته شمرده نمی‌شوند).
- پنج خانه‌ی باز همان پنج قرعه‌اند: ویدیو loss60/extreme، صوت loss60،
  پیام loss60/extreme — حالا برای اولین بار روی مسیر پاک قرعه‌کشی
  واقعی می‌شوند.
- نقشه‌ی راه تا پایان (خواسته‌ی صریح کاربر، ۲۰۲۶-۰۸-۰۹):
  ۱) هر ۸ ردیفِ هر سه ماتریس سبز → ۲) فاز تنظیمات و ظاهر →
  ۳) مرحله‌ی آخر: بررسی سند پژوهشی یکسان‌سازی رفتاری در
  `docs/TRAFFIC_NORMALIZATION_RESEARCH.md` (پنج پرسش بخش ۱۱ و قید
  «خاموش‌شدن روی لینک باریک» در RIG_GUIDE بخش ۶ ثبت است).

---

## ۱. کارنامه‌ی اندازه‌گیری‌شده (سه ماتریس)

### ماتریس صوت — sla_thresholds_test.dart

```
clean      PASS         connect 2.9s
normal     PASS         connect 6.1s
latency    PASS         connect 29.0s
bandwidth  PASS         connect 6.0s
narrow     PASS         connect 12.2s
loss10     PASS         connect 5.1s
extreme    PASS/STRESS  connect 63.7s  recovered 7.5s  alive
loss60     OPEN         اتصال اثبات شد (۱۸۶s + ۵۳هزار بسته) اما ردیف سبز ثبت نشده
```

شش ردیف بالا در یک اجرای واحد سبز شدند. extreme جداگانه سبز شد.

### ماتریس پیام‌رسانی — messaging_survival_test.dart

```
clean      PASS   متن p50 ۲ms · عکس ۳۸ms · ویس ۱۱ms
normal     PASS   متن p50 ۸۹ms · عکس ۶۲۷ms · ویس ۲۶۱ms
latency    PASS   (پس از اصلاح پنجره‌ی ack هر chunk)
bandwidth  PASS   عکس ۴۸KB روی ۳۲kbps در ۳۱s
narrow     PASS   عکس ۱۱۰s · ویس رسید — با استریم ack-paced و pacing درین-آگاه
loss10     PASS
loss60     OPEN   وابسته به همان قرعه‌ی اتصال صوت
extreme    OPEN   وابسته به همان قرعه‌ی اتصال صوت
```

### ماتریس ویدیو — video_survival_test.dart (لاین باینری تازه)

```
clean      PASS   ۶۴MiB سالم و sha-تأییدشده در ۳۲.۷s (۱۶٬۴۳۱ kbps)
normal     PASS   ۶۴MiB در ۲۲۳.۸s (۲٬۳۹۹ kbps)
loss10     PASS   ۴MiB در ۷۴.۰s (۴۵۴ kbps)
bandwidth  PASS   ۸۶۴KB در ۵۰۷.۵s (۱۴ kbps) — با فشار برگشتی واقعی
latency    PASS   ۸MiB sha-تأییدشده در ۴۹۰s (۱۳۷ kbps، صفر resume) — ۲۰۲۶-۰۸-۰۸
narrow     PASS   ۵۱۲KiB sha-تأییدشده در ۱۱۴۱s (۴ kbps، صفر resume، تماس ۲۳ دقیقه زنده)
loss60     OPEN   شش ارتقای ساختاری در ۲۰۲۶-۰۸-۰۸/۰۹ نشسته (پایین)؛ نردبان
                  مرگ از allocate تا media بالا آمد؛ دور آخر قرعه‌ها به‌خاطر
                  خرابی فیزیکی لینک پایه باطل شد (بلاکر پایین)
extreme    OPEN   وابسته به همان واحد؛ پس از سبزی loss60
بازتأیید clean/normal/loss10/bandwidth با sender جدید: هر چهار سبز
(normal بار اول خطای گذرای اتصال داشت، تکرارش ۶۴MiB در ۵۳s سبز شد)
```

شش ارتقای loss60 (همه با ۱۶۲/۱۶۲ تست call_core سبز، آنالایزر تمیز):

- ورودی دوم ICE با transport=tcp + شکل‌دهی صادقانه‌ی همان پورت TCP
- قاضی واحد: تایم‌اوت ارسال در اتصال اولیه هرگز سوکت را نمی‌چرخاند
- بدون صبر تازه: تایم‌اوت نرم واچ‌داگ را دوباره کوک نمی‌کند
- تفکیک تایپ‌دار EngineTimeoutException — تایم‌اوت انجین سختِ فوری
- مهلت رگبار media_disconnected (connectionTimeout/4، کف ۲s سقف ۲۰s)
- مالکیت تک‌نفره‌ی generation در اتصال اولیه (گیرنده فقط درخواست/پاسخ)

درس ریگ (۲۰۲۶-۰۸-۰۹): وای‌فای گوشی با قفل صفحه به doze می‌رود و حتی
وسط ران، ۸۰-۹۰٪ اتلافِ خام روی شکل‌دهی سوار می‌کند؛ بیدارنگه‌دار ICMP
هم خودش شکل می‌شود و می‌میرد — راه درست جریان UDP به پورت 5353 است که
قواعد شکل‌دهی عمداً مستثنایش کرده‌اند (اسکریپت wake_udp.py در پتک).

طرح کدآماده‌ی ذخیره — «صندلی چند-سوکته» (فقط اگر دور تمیز پتک قرمز شد):

- سرور relay_server.dart: `members: Map<String, WebSocket>` →
  `Map<String, List<WebSocket>>` با سقف ۳؛ join هم-هویت به‌جای supersede
  اضافه می‌کند (بالای سقف: قدیمی‌ترین بسته می‌شود)؛ relayOrBuffer به
  همه‌ی سوکت‌های صندلیِ مقابل fan-out می‌کند؛ مرگ سوکت فقط همان سوکت را
  حذف می‌کند و صندلی وقتی خالی می‌شود که لیستش تهی شود؛
  `members.length` همه‌جا یعنی تعداد صندلی، نه سوکت.
- کلاینت signaling_client.dart: پارامتر hedgeConnections (پیش‌فرض ۱؛
  ریگ پرتلفات ۲-۳)؛ هر اتصال join خودش را می‌فرستد؛ outbox هر envelope
  را روی همه‌ی اتصال‌های باز شلیک می‌کند؛ ورودی‌ها merge می‌شوند —
  dedupe از قبل در adapter هست (تست flapping duplicates).
- چرا نه K-کپی روی یک سوکت: کپی‌ها روی یک جریان TCP پشت همان stall صف
  می‌شوند؛ استقلالِ سرنوشت فقط با جریان‌های مستقل می‌آید.
- گیت: تست‌های server (۱۹) + signaling (۹۵) + سناریوی جدید «یک جریان
  stall شده، جریان دوم تحویل می‌دهد»، سپس ران ریگ.

نتیجه‌ی نهایی مأموریت شبانه (۲۰۲۶-۰۸-۰۹، ۲۲ قرعه، همه خودکار):

```
video  loss60   8 شلیک فقط-اتصال: هر ۸ INVALID/UNROUTED — مرگ یک‌طرفه‌ی
                رادیوی AP وسط ران (گوشی می‌فرستد، مک نمی‌رسد)
voice  loss60   6 قرعه FAIL/STRESS؛ قرعه‌ی ۲ با ۲۹هزار بسته واقعاً جنگید
                و فقط بودجه‌ی connect/recovery زیر فیزیک ۶۰٪ تمام شد
msg    loss60   4 قرعه FAIL/STRESS (رادیو در ۳ قرعه ریخت)
msg    extreme  4 قرعه FAIL/STRESS؛ سه قرعه با ۵-۸هزار بسته یعنی رادیو
                ایستاد و فیزیک extreme (rtt ~2s + اتلاف) نگذاشت
```

دو علت مستقل در داده‌ها تفکیک شد: اکثریت قرعه‌ها قربانی ناپایداری
دقیقه‌ای رادیوی AP مک شدند (ردیف‌های INVALID یعنی ترافیک اصلاً از پل
نگذشت — تقصیر پروتکل نیست)، و اقلیتِ رادیو-پایدار به فیزیک واقعی
پروفایل خوردند که یعنی بودجه/قرعه‌های بیشتر یا AP پایدار لازم است.

تجویز قطعی برای بستن پنج خانه‌ی باز: AP پایدار (uplink سیمی مک یا
اکسس‌پوینت اختصاصی) — رادیوی واحد مک که هم کلاینت خانه است هم AP،
شب‌ها بازه‌ی سالمِ ۵دقیقه‌ای هم نگه نمی‌دارد. پس از پایداری، کل خط با
یک فرمان: bash <scratchpad>/night_orchestrator.sh

مأموریت شبانه‌ی ۲۰۲۶-۰۸-۰۹ (کاربر خواب؛ اجرای خودمختار در جریان):

- پشته به نُه ارتقا رسید — سه‌تای تازه‌ی امروز: صندلی چند-سوکته در رله
  (سقف ۳ سوکت هر هویت، fan-out کامل، ۱۹/۱۹)، سوکت‌های hedged در کلاینت
  سیگنالینگ (پارامتر hedgeSockets، پیش‌فرض صفر، ۹۵/۹۵؛ فعال روی
  loss>=0.3)، و شمارنده‌ی UNROUTED که مسیر TCP رله را هم شواهد می‌شمارد.
- خط تک‌تیرانداز: sniper.sh — قرعه‌های ارزانِ فقط-اتصال
  (loopback_call_test) و پس از اولین سبز، ران کامل ویدیو برای مهر رکورد؛
  loss60 سپس extreme.
- خط شبانه: overnight.sh — پس از ویدیو: صوت loss60 (آیتم ۸، تا ۶ قرعه)،
  پیام‌رسانی loss60 و extreme (تا ۴ قرعه هرکدام)؛ توقف در اولین سبزِ هر
  ردیف؛ دروازه‌ی پنجره‌ی تمیز + بیدارنگه‌دار UDP-5353 در هر دو.
- هدف اعلام‌شده‌ی کاربر: ۸/۸ ویدیو سبز، پیام‌رسانی کامل (متن/ویس/عکس/
  فیلم) و تماس صوتی دوطرفه زیر سخت‌ترین شرایط؛ سپس تنظیمات/ظاهر.

پروتکل ازسرگیری پس از کرش:

```
cat <scratchpad>/sniper_run.out <scratchpad>/overnight_run.out
bash <scratchpad>/overnight.sh    # خودش منتظر sniper می‌ماند اگر زنده باشد
```

سبز شدن narrow پس از هفت ران، با پنج مؤلفه‌ی اندازه‌گیری-محور:

- ران تشخیصی بدون صوت PASS داد → قاتل، هزینه‌ی سیمی صوت بود نه لاین
- حساب چهار عبورِ جریان: صوتِ دوطرفه روی لوله‌ی ۳۲k حتی با کف Opus
  (۶k@120 = ۸.۷k سیم × ۴ = ۳۴.۷k) فیزیکاً جا نمی‌شود؛ pcap ران ۶ همین
  را نشان داد (~۴۴k روی لوله‌ی ۳۲k) و میکروفن باز، DTX را بی‌اثر می‌کند
- مدل OpusWireBudget پارامتر concurrentStreams گرفت + DTX فعال شد
- گیرنده روی لینک‌های bw-محدود noLocalAudio می‌شود (شنونده‌ی ساکت —
  همان که DTX با هدست واقعی می‌ساخت): صوت یک‌طرفه ۱۷.۳k + لاین ۸k ≈
  ۲۵k از ۳۲k → ~۲۲٪ headroom برای کنترل
- سهمیه‌ی لاین: sender حالا token-bucket زنده دارد
  (sendBudgetBytesPerSec؛ باگ سقف burst زیر یک chunk در ران ۵ کشف و
  با تست رگرسیون میخ شد)؛ تست ویدیو ۲۵٪ سهم per-crossing می‌دهد

واحد محصولیِ باز (ثبت برای فاز بعد): همین منطق «صوت جا نمی‌شود →
یک‌طرفه/بی‌صوت» باید به‌صورت رانگِ خودکارِ MediaAdaptationDriver در خود
محصول هم بیاید — الان فقط در پیکربندی تست ریگ است.

سبز شدن latency حاصل چهار ارتقای اندازه‌گیری-محور در sender (پنج ران ریگ):

- دروازه‌ی فشار برگشتی از ثابت به مشتق از پنجره (ران ۱: ۳۰۸/۵۱۲)
- قانون Karn + مهلت تطبیقی RFC6298 (ران ۲ شواهد داد: srtt=0، retx=391)
- backoff بوت‌استرپ مهلت + کف مشتق از RTT در تست (ران ۳-۴: retx→۱۱،
  اتصال ۱۶۰s→۲۱s، اما پنجره روی کف میخ‌کوب)
- پنجره‌ی AIMD به‌جای آینه‌ی BDP خودارجاع (ران ۵: PASS)

درس ثبت‌شده:

```
~/.claude/knowledge/lessons/networking/rtt-sampling-karn.md
```

---

## ۲. آنچه در محصول ساخته شد (همه با تست واحد سبز)

- تایم‌بندی سیگنالینگ مشتق از شرایط شبکه (heartbeat / liveness / dial / attempts)
- انقضای صف ارسال هم‌قد بودجه‌ی reconnect
- واچ‌داگ اتصال اولیه + بازیابی پیشرفت-آگاه در هر سه فاز عمر تماس
- اتاق هویت-محور در سرور relay با ازسرگیری نشست و آزادسازی صندلی
- شماره‌گیری موازی تطبیقی (سه قرعه فقط روی لینک پرتلفات)
- ترجیحات Opus داخل SDP ارسالی + کاندیدهای ptime واقعی انکودر
- استریم ضمیمه‌ی ack-paced با pacing درین-آگاه
- لاین باینری ویدیو: پنجره‌ی BDP-سنج، pacing با سیگنال صف، ازسرگیری محتوامحور،
  توقف در recovery، و فشار برگشتی از `bufferedAmount` واقعی کانال

تست‌ها: call_core ۱۶۱ · signaling ۹۵ · server ۱۹ · messaging ۴۶ ·
media_webrtc ۹۷ · adapter ۱۰ — همه سبز. آنالایزر همه‌ی پکیج‌ها و اپ: تمیز.

---

## ۳. قدم بعدی (دقیق، آماده‌ی اجرا)

### قدم ۳.۱ — رفع ردیف latency ویدیو — کد انجام شد، گیت ریگ مانده

اصلاح اعمال شد (۲۰۲۶-۰۸-۰۸، بک‌آپ ۲۸۱ و ۲۸۲):

- آستانه‌ی دروازه‌ی فشار برگشتی در `sendChunk` دیگر ثابت نیست؛ از
  پنجره‌ی زنده‌ی BDP-سنج مشتق می‌شود، با کف دو chunk و یک chunk
  سرریز برای framing:

```
gate = (max(window, 2) + 1) * chunkBytes
```

- پنجره به بالای `sendChunk` منتقل شد (یک binding مشترک؛ حلقه‌ی
  بیرونی فقط مقداردهی می‌کند، نه اعلان مجدد — دام shadowing در کامنت
  کد ثبت است) و شرط دروازه در هر poll زنده خوانده می‌شود.
- دو تست واحد خاصیت را میخ کردند: مقیاس‌گیری با پنجره + پایداری کف.

راستی‌آزمایی انجام‌شده: آنالایزر تمیز، ۴۸/۴۸ تست پکیج messaging سبز.

بلاکر تاریخ‌دار (۲۰۲۶-۰۸-۰۸): لینک اترنت ریگ قطع است —
`ifconfig en0` → `status: inactive`؛ گوشی روی پل نیست (ARP ناقص،
loss ۱۰۰٪). AP یا کابل باید وصل/روشن شود؛ از نرم‌افزار قابل احیا
نیست. پس از وصل شدن لینک، گیت ردیف latency:

```
sudo -n T2_PEER=192.168.3.3 tools/t2/h2_run.sh latency integration_test/video_survival_test.dart 00008030-001215003AF2802E
```

پس از سبز شدن، ردیف‌های سبز قبلی ویدیو (clean/normal/loss10/bandwidth)
هم باید دوباره سبز شوند — دروازه‌ی جدید روی لوله‌ی باریک عمیق‌تر از
دروازه‌ی قدیم اجازه‌ی صف می‌دهد و باید اثباتش ثبت شود.

### قدم ۳.۲ — ردیف narrow ویدیو: واحد شکست‌خورده «بقای تماس»، نه sender

سه ران (۲۰۲۶-۰۸-۰۸)، سه شکست با یک امضا — تماس حین انتقال می‌میرد:

```
run1  helloAcked=false  acked=0   ch=[]                    phases=failed/failed
run2  helloAcked=true   acked=5   srtt=36424 retx=5  ch=[open,closing,closed]
run3  helloAcked=true   acked=15  srtt=36563 retx=7  ch=[open,closing,closed]
      (run3 با گیت درین-محور + chunk چهارکیلوبایتی — باز هم srtt~36s)
```

استدلال قاطع: در ران ۱ لاین هرگز باز نشد و صفر بایت ویدیو رفت، اما تماس
باز هم در دقیقه‌ی ~۳ مُرد (pcap: ترافیک ۱۶:۲۸-۱۶:۳۱، بعد سکوت؛ تلاش‌های
recovery در ۱۶:۳۷-۱۶:۴۱ ناموفق). پس قاتلْ لاین نیست — صوتِ واقعی روی
لینک ۱۶kbps خودِ لینک را oversubscribe می‌کند و heartbeat/liveness خفه
می‌شود. srtt~۳۶s در ران ۲/۳ با وجود سقف ۸KB صف لاین یعنی صف در
SCTP/TURN مشترک است، نه در backlog لاین.

پرسش‌های باز که ران بعدی را تعیین می‌کنند:

- ردیف narrow ماتریس صوت با کدام mediaMode سبز شد؟ (ویدیو realAudio بود)
- ترجیحات Opus (bitrate/ptime) در SDP روی narrow واقعاً چه مقداری اعمال شد؟
- تست تشخیصی: video narrow با صوت loopback → اگر سبز شد، واحد اصلاح
  «سهم‌بندی نرخ بیت صوت با ظرفیت لینک» است (cap پویا از conditions).
  پیش‌نیاز پیاده‌سازی: `resolveMediaMode` در
  `integration_test/support/e2e_support.dart:394` فقط کاوش میکروفن است و
  override ندارد — یک `String.fromEnvironment('E2E_MEDIA_MODE')` باید
  اضافه شود تا `noLocalAudio` قابل تحمیل باشد (تغییر کوچک، اثر صفر بر
  ردیف‌های سبز چون پیش‌فرض همان کاوش می‌ماند).
- نکته‌ی مقایسه: هر سه ردیف سبز narrow (صوت/پیام‌رسانی) هم realAudio
  بوده‌اند اما عمرشان کوتاه‌تر از ~۳ دقیقه بود؛ مرگ تماس در ویدیو حدود
  دقیقه‌ی ۳ رخ می‌دهد — پس «مدت بقا زیر اشباع» متغیر جداکننده است، و
  سؤال درست این است که چه چیزی پس از ~۳ دقیقه صف انباشته را به آستانه‌ی
  liveness می‌رساند.

### قدم ۳.۲ب — پس از narrow: loss60 و extreme

### قدم ۳.۳ — بستن ردیف loss60 صوت

اتصالش اثبات شده اما ردیف کاملِ سبز ثبت نشده. اجراهای پیاپی لازم است؛
هر ران حدود ۱۵ دقیقه.

### قدم ۳.۴ — ویدیوی کامل ۳ دقیقه‌ای

گیت فعلی روی هر پروفایل یک نمونه‌ی متناسب با فیزیک لینک می‌فرستد. برای
کلیپ کامل (۳۵۰ تا ۴۵۰ مگابایت) یک seam منبع جریانی لازم است تا فایل به
جای بارگذاری کامل در حافظه، تکه‌تکه از دیسک خوانده شود — چون هر دو طرف
تماس در این ریگ روی یک گوشی زندگی می‌کنند.

---

## ۴. نکات عملیاتی

- گوشی: `00008030-001215003AF2802E` روی `192.168.3.3`، پل `bridge100`
- گزارش تصویری: `python3 tools/t2/report_matrix.py` → `tools/t2/h2_report.html`
- جدول خام: `tools/t2/h2_results.tsv`
- بک‌آپ‌های شماره‌دار: `.backups/` تا شماره ۲۸۰

## ۵. توضیح فلگ‌های نشست

هشدارهای `clarity guard` در این نشست به تراکم واژگان حوزه‌ی زیرساخت
شبکه در یک گفتگوی طولانی برمی‌گردد، نه به کار یا کاربر. راه عملی:
موضوع تازه در نشست تازه، و فشرده‌سازی تاریخچه با `/compact`.
