name: signed_config
description: >-
  Signed HTTPS endpoint manifest: model, Ed25519 signature verification
  against build-pinned keys, and a verified cache with rollback protection
  and last-known-good fallback.
version: 2.0.0
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  clock: ^1.1.1

dev_dependencies:
  test: ^1.25.0
  fake_async: ^1.3.1
