name: security
description: >-
  Device identity (Ed25519 via an audited-crypto adapter, TOFU pinning,
  safety numbers) and mandatory log redaction.
version: 2.0.0
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dev_dependencies:
  test: ^1.25.0
  fake_async: ^1.3.1
