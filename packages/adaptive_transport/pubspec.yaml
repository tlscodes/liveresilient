name: adaptive_transport
description: >-
  Multi-path transport abstraction with EWMA health scoring, adaptive
  routing, fanout redundancy, and circuit breaking. Standards-based only.
version: 2.0.0
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  clock: ^1.1.1
  crypto: ^3.0.3
  cryptography: ^2.7.0
  security:
    path: ../security
  signed_config:
    path: ../signed_config

dev_dependencies:
  test: ^1.25.0
  fake_async: ^1.3.1
