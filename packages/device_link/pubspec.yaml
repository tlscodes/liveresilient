name: device_link
description: >-
  Consent-gated nearby-device connectivity for degraded-mode operation:
  Ed25519-authenticated envelopes, replay rejection, and an opt-in,
  off-by-default forwarding processor.
version: 2.0.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  adaptive_transport:
    path: ../adaptive_transport
