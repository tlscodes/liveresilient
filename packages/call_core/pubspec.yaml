name: call_core
description: >-
  Call session orchestration: platform-independent call state machine,
  controller, and ICE-restart reconnect policy.
version: 2.0.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
