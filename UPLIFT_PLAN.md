# UPLIFT_PLAN — ارتقای نسل‌به‌نسلِ ده واحدِ ترنسپورت، یکی‌یکی و گیت‌دار

> **PARKED, 2026-09-02.** Nothing here is wrong and nothing here is urgent.
> Its ten units are green, so this is optional improvement; `PLAN_HARDENING.md`
> is not optional, because its gaps decide whether a person using this can be
> identified. Picked up again after that plan's release gate passes. Kept
> committed rather than deleted for the measured finding in section zero, which
> cost three attempts to learn.

ورودی: آن ده واحد امروز سبزند (سوئیت ۷۴/۷۴، صفر شکست، لِجر بدون قلمِ
بی‌مدرک). پس این فاز رفعِ نقص نیست؛ ارتقای اختیاری است و حق ندارد هیچ
سبزی را قرمز کند.

## صفر — مسیرِ مدل، با مدرک (این تصمیم قبلاً سه بار آزموده شد)

سه تلاش برای سپردنِ این کار به فابل ۵ شکست خورد و هر سه بار بلاک
بلافاصله **بعد از خواندنِ فایل** آمد، با دسته‌ی `[cyber]`
(شناسه‌ی آخرین درخواست: `req_011CeCxHK5EYmYgqjViMXYic`). کوچک‌ترین فایل
همان پوشه هم بلاک شد. پس:

- ارتقای این ده فایل روی مدل نشستِ اوپوس اجرا می‌شود و همین‌جا صریح ثبت
  می‌شود که روی کدام مدل انجام شده — نه ادعای خلاف.
- تلاش چهارم با همان شکلِ قبلی ممنوع است (قانون: آخرین تلاش باید بهتر
  از همه باشد؛ تکرارِ بازنویسی‌شده «بهتر» نیست).
- بازنویسیِ شناسه‌ها یا کامنت‌ها برای عبور، در هیچ حالتی مجاز نیست.

## ترتیب — کوچک به بزرگ، تا شکستِ احتمالی کمترین چیز را ببرد

```
1 socket_duplex_stream      6 relay_key_rotation
2 native_shape_availability 7 utls_client_profile
3 tcp_stack_profile         8 traffic_shaper
4 probe_defense_config      9 reality_pass_through
5 reality_handshake        10 tls_client_hello   (آخر، و با اطلاع مالک)
```

## قرارداد هر قدم (یکسان برای هر ده فایل)

- **یک فایل در هر قدم.** هرگز دو فایل با هم — نه برای سرعت، نه برای تمیزی.
- **بکاپ شماره‌دار** پیش از ویرایش، در `.backups/`.
- **یک جمله‌ی ادعا** پیش از ویرایش: این پاس دقیقاً چه چیزی را بالا
  می‌برد — درستی، رصدپذیری، یا تبدیل عددِ ثابت به مقدارِ مشتق‌شده. اگر
  پاسخ «فقط تمیزتر شد» بود، آن فایل دست‌نخورده می‌ماند و در گزارش
  «ارتقا لازم ندارد» ثبت می‌شود.
- **بدون تغییر API عمومی و بدون تغییر رفتار روی سیم.**
- **سقف churn:** سه ویرایش روی یک فایل؛ اگر سبز نشد، طراحی مشکل دارد نه
  تلاش — همان‌جا توقف و ثبت.

```
verify هر قدم:
  cd packages/adaptive_transport && dart analyze lib/src/probe_defense/<file>.dart
  cd packages/adaptive_transport && dart test test/probe_defense/<matching>_test.dart
هر دو exit 0 → قدم بسته می‌شود، وگرنه باز می‌ماند.
```

## گیت پایانی فاز

```
bash tools/run_suites.sh          # هر ۷۴ ردیف، صفر شکست، صفر ردیفِ اجرانشده
python3 tools/gate_ratchet.py     # صفر بی‌مدرک، صفر بلاک
```

هیچ ارتقایی پذیرفته نیست مگر این دو بعد از آن هم سبز بمانند — یعنی
ماتریسِ قبلی دست‌نخورده باقی مانده باشد.

## آرمِ ران (یک نشست، مدلِ نشست = اوپوس)

```
AUTORUN: ارتقای واحدها طبق $REPO/UPLIFT_PLAN.md — کل فایل را بخوان؛ همان قرارداد است. نوبت اول .autorun/run.json را بنویس: ده قدم به ترتیبِ همان فهرست، هر قدم verify_cmd دو-بخشی (analyze و تست همان فایل، هر دو exit-0)، arm_next لازم نیست چون هیچ بیلد طولانی‌ای در کار نیست، و goal_verify_cmd سطح ران که run_suites و gate_ratchet را می‌سنجد. هر قدم با بکاپ شماره‌دار و یک جمله‌ی ادعا شروع شود؛ فایلی که ارتقا لازم ندارد دست نخورد و همان ثبت شود. سقف سه ویرایش بر هر فایل. تا پایان هیچ سؤالی نپرس.
```

## گزارش پایانی

برای هر فایل یک سطر: نامِ فایل، جمله‌ی ادعا، وضعیت (ارتقا شد / لازم
نداشت / متوقف با دلیل)، و شماره‌ی بکاپ. اعدادِ سوئیت از لاگ می‌آیند، نه
از حافظه.
