name: reference_app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  meta: ^1.15.0

  # VoiceCallKit v2 stack (standalone path deps; the app and the
  # media_webrtc_flutter plugin package stay OUT of the root pure-Dart pub
  # workspace so Flutter SDK constraints never fight the root resolution).
  adaptive_transport:
    path: ../../packages/adaptive_transport
  broadcast:
    path: ../../packages/broadcast
  call_core:
    path: ../../packages/call_core
  call_media_adapter:
    path: ../../packages/call_media_adapter
  call_signaling_adapter:
    path: ../../packages/call_signaling_adapter
  connection_orchestrator:
    path: ../../packages/connection_orchestrator
  on_device_assistant:
    path: ../../packages/on_device_assistant
  device_link:
    path: ../../packages/device_link
  media_webrtc:
    path: ../../packages/media_webrtc
  media_webrtc_flutter:
    path: ../../packages/media_webrtc_flutter
  # Carries PtTransport.xcframework into the Apple bundles; no Dart API of its
  # own. Non-Apple builds ignore it — it declares only ios and macos.
  pt_transport_darwin:
    path: ../../packages/pt_transport_darwin
  signaling:
    path: ../../packages/signaling
  # For the manifest's ICE servers: without them every call is placed with an
  # empty STUN/TURN list.
  signed_config:
    path: ../../packages/signed_config
  live_captions:
    path: ../../packages/live_captions
  messaging:
    path: ../../packages/messaging
  messaging_webrtc_adapter:
    path: ../../packages/messaging_webrtc_adapter
  file_selector: ^1.1.0
  image: ^4.9.1
  image_picker: ^1.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

  # Deterministic time control for the chat retry-driver test: fakeAsync
  # drives the app's periodic Timer AND (via package:clock's zone clock)
  # the messenger's retryAfter window without real waiting.
  fake_async: ^1.3.1

  # The broadcast publisher and reader take their time from package:clock's
  # zone clock, so the wiring test pins an instant with Clock.fixed instead
  # of depending on when the suite happens to run.
  clock: ^1.1.1

  # Direct dev-dep (same constraint as media_webrtc_flutter): the E2E
  # support code probes `getUserMedia` directly to report the media mode.
  flutter_webrtc: ^1.5.2

  # Dev-only, and deliberately so: nothing the app ships imports this. One
  # integration test asks the running process whether the pinned backend is
  # linked into it and what bytes it composes, which is a measurement of THIS
  # build on THIS architecture and cannot be made from the host.
  native_transport:
    path: ../../packages/native_transport

  # In-process TLS signaling relay for the on-device loopback E2E suite
  # (integration_test/): the tests bind the real relay inside the app
  # process so the full stack runs over a real wss:// socket on one machine.
  signaling_server:
    path: ../../server/signaling_server

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
