# راهنمای کامل ریگ، وضعیت و فرماندهی — voice_call_kit_v3

## ۰. نمودار رشد زنده (با هر رویداد به‌روز می‌شود)

```
هدف نهایی: 24/24 سبز
├─ VIDEO  [########] 8/8   کامل! (loss60 با لاین فواره‌ای روی دیتاگرام، ۲۰۲۶-۰۸-۱۰)
├─ VOICE  [########] 8/8   کامل! (آیتم تاریخی ۸ بسته شد ۲۰۲۶-۰۸-۱۰)
└─ MSG    [########] 8/8   کامل! (loss60 و extreme هر دو ۲۰۲۶-۰۸-۱۰)
جمع سبز: 24/24 — ماتریس کامل! فاز بعدی: «تنظیمات و ظاهر»، سپس سند پژوهشی بخش ۶
```

- ۲۰۲۶-۰۸-۱۰ ~۰۵:۳۰ — 🎉🎉 ویدیو loss60 PASS/STRESS رسمی = ۲۴/۲۴ کامل.
  مسیر برنده دقیقاً پلن §۰.۳: لاین فواره‌ای روی ترنسپورت دیتاگرام
  (رله‌ی گنگ UDP:3737 روی مک، پورت اپ بدون هیچ CC زیرین). حکم رسمی:
  4194304B تحویل sha-تأییدشده در 303564ms (۱۱۱kbps) روی ۱۴ پرومکس؛
  سربار نماد 10413/4096 = 2.54x (کف نظری 2.5x در ۶۰٪ — تقریباً بهینه)؛
  صفر افت محلی، صفر recovery-freeze؛ هم‌خوان با پروب مک (۳۰۱s).
  دو ران قبلی روی کانال دیتای SCTP هر دو FAIL با یک امضا بودند — شرح
  کامل فروپاشی SCTP-CC و شواهدش در §۰.۶؛ درمان = دیتاگرام، نه پارامتر.
  سه سخت‌سازی هم‌نشست: کف نرخ همیشه-فعال در لاین (پروب: ۴۵۶s→۳۰۱s)،
  انقضای پارامتری گیرنده، عبور maxRetransmits:0 از پورت فلاتر (تله‌ی
  toMap بالادستی) — همه تست-پین. دروازه‌های جدید ریگ: echo-پروب رله‌ی
  دیتاگرام قبل از هر شات (تله‌ی alt-port کوترن روی 3479 → پورت 3737).
  تست‌ها: relay ۵/۵ نو · messaging ۵۷/۵۷ · media_webrtc ۹۸/۹۸ ·
  media_webrtc_flutter ۱۷/۱۷ · پروب ۴MiB مک سبز. بک‌آپ ۳۱۸-۳۲۸؛ لاگ
  video_loss60_datagram_official.out؛ جدول رسمی h2_results.tsv.

- ۲۰۲۶-۰۸-۱۰ ~۰۴:۱۰ — 🎉 ویدیو extreme PASS (قرعه‌ی ۱ ثبتی): کلیپ 4MB
  کامل تحویل و sha-تأیید زیر rtt≈2s + 16kbps + ۱۵٪ اتلاف در ۱۵۰۵
  ثانیه — پروب‌های RTT لاین دقیقاً این را ممکن کردند. FINALE_DONE:
  نتیجه‌ی خط پنج‌ردیفی = ۳ سبز تازه (صوت loss60، پیام loss60، پیام
  extreme) + ویدیو extreme = ماتریس از ۱۹ به ۲۳/۲۴ در یک شب. بلوکر
  باقی‌مانده فقط ویدیو loss60 است (نه extreme) — دقیقاً همان خانه‌ای
  که معماری فواره‌ای برایش مصوب شد.

- ۲۰۲۶-۰۸-۱۰ ~۰۲:۱۵ — 🎉 پیام extreme هم PASS (قرعه‌ی ۲): همه‌ی
  پیام‌ها سالم زیر rtt≈2s + 16kbps + ۱۵٪ اتلاف. ردیف پیام ۸/۸ شد؛
  ماتریس ۲۲/۲۴. مانیتور سلامت مسیر هم به مدل بیس‌لاین-نسبی ارتقا یافت
  (بک‌آپ ۳۱۵-۳۱۶) تا پروفایل‌های ذاتاً پراتلاف را «ناسالم» نشمارد.

- ۲۰۲۶-۰۸-۱۰ ~۰۱:۳۰ — 🎉 پیام loss60 PASS در همان قرعه‌ی اول: همه‌ی
  پیام‌ها تحویل سالم (۳۹۷۷۱۲ بسته، ۳۱ دقیقه) — پروب‌های RTT لاین
  باینری زیر اتلاف سنگین دقیقاً همین‌جا خودشان را ثابت کردند.

- ۲۰۲۶-۰۸-۱۰ ~۰۰:۵۵ — 🎉 صوت loss60 PASS/STRESS رسمی: اتصال 4454ms،
  ack p50/p95 = 170/537ms، ack loss صفر، بازیابی 844ms، alive —
  آیتم ۸ که از آغاز پروژه باز بود بسته شد. ردیف با هشت فیکس امروز +
  فیزیک برچسب-صادق گرفته شد (جدول رسمی h2_results.tsv).

- ۲۰۲۶-۰۸-۰۹ ~۰۸:۵۰ — کشف بزرگ: دو آیفون در کار بوده! کل ریگ به
  آیفون ۱۱ (00008030-...) نشانه می‌رفت؛ دستگاهِ روی کابل حالا آیفون
  ۱۴ پرومکس است (00008120-0010795A3613C01E) — خط بازنشانه‌گذاری شد.
- توپولوژی نهایی: اینترنت مک از وای‌فای خانه (en1) + داده‌ی تست از
  کابل USB (en6، گوشی 169.254.209.150، ~۰.۹ms، صفر اتلاف).
- وضعیت جاری: اولین بیلد/نصب اپ روی ۱۴ پرومکس در جریان است؛ در
  اولین اجرا پنجره‌ی مجوز میکروفن روی گوشی می‌آید — یک بار Allow.
- ۲۰۲۶-۰۸-۰۹ ~۰۹:۴۰ — علت کندی پیدا و رفع شد: ران‌های کهنه هنوز
  آیفون ۱۱ و آدرس‌های هات‌اسپات قدیمی را هدف می‌گرفتند (پنج بار
  DENIED)؛ همه کشته شدند و خط از نو روی لینک USB روشن شد. رگ تازه
  سبز بالا آمد: STUN و TURN روی 169.254.110.17 تخصیص گرفت؛ شات ۱
  از loss60 با شرایط rtt=4ms و loss=0.60 در جریان است.
- ۲۰۲۶-۰۸-۰۹ ~۱۱:۰۰ — ازسرگیری با گوشی ۱۱ پرومکس (بدون سیم) روی کابل:
  اشتراک اینترنت iPhone USB از قبل روشن بود، کشف خودکار سبز
  (bridge100 / 192.168.2.2 / 00008030-...، پینگ 1.2ms و ۰٪).
  سه UDID هاردکدِ ۱۴ پرومکس در sniper.sh و overnight.sh پارامتری شد
  (DEVICE اجباری، UNKNOWN=خطای قطعی؛ بک‌آپ ۳۰۱-۳۰۲). ارکستریتور از
  repo روشن شد؛ شات ۱ ویدیو loss60 روی گوشی ۱۱ در جریان است.
- ۲۰۲۶-۰۸-۰۹ ~۱۶:۰۰ — لحظه‌ی تاریخی: اولین پاس کامل زیر loss60 در کل
  عمر پروژه. suite لوپ‌بک با فیزیک برچسب-صادق «All tests passed»:
  اتصال هر دو سر در ۲۷ ثانیه (۳۲ CreatePermission، کاندیدای relay هر
  دو سمت)، جریان RTP با شمارنده‌های صعودی، بازیابی ری‌استارت ICE،
  hangup تمیز. ترکیب برنده: ۷ فیکس محصول + بودجه‌ی واقعی + پایپ
  1-sqrt(1-P). قرعه‌ی ارزان دیگر لازم نیست — خط finale برای پنج ردیف
  رسمی روشن شد (tools/t2/pipelines/finale.sh، لاگ finale_run.out).
- ۲۰۲۶-۰۸-۰۹ ~۱۳:۳۰ — چرخش بزرگ: دو قرعه‌ی کامل ۴۵۰ثانیه‌ای با تخصیص
  TURN موفق ولی صفر ترافیک رله نشان داد «قرعه» نیست، باگ ساختاری است.
  شکار چندعاملی (۱۳ عامل، راستی‌آزمایی خصمانه) شش باگ تأییدشده یافت و
  هر شش در سه واحد فیکس شد — شرح کامل در بخش ۰.۶؛ تست‌های هر سه پکیج
  سبز. خط برای قرعه‌گیری با کد نسل جدید دوباره روشن شد.
- شمارش قرعه‌های ویدیو loss60 روی گوشی ۱۱ (پس از اصلاح بودجه):
  قرعه‌ی ۱ FAIL/STRESS (۵۷۵۶ بسته، ۴۵۲ ثانیه، اتصال نگرفت)؛ سپس خط برای
  شکار باگ متوقف شد — قرعه‌های بعدی با کد فیکس‌شده از نو.
- ۲۰۲۶-۰۸-۰۹ ~۱۱:۲۵ — ریشه‌ی UNROUTEDهای گوشی ۱۱ پیدا و بسته شد: در
  loopback_call_test سقف اتصال ۳۰ ثانیه هاردکد بود و بودجه‌ی ۴۵۰ثانیه‌ای
  STRESS هرگز به تست نمی‌رسید — هر قرعه در ثانیه‌ی ۳۰ می‌مرد و بسته‌ی
  کافی رد نمی‌شد. حالا مثل تست‌های survival از E2E_CONNECT_BUDGET_S و
  E2E_STRESS_CONNECT_S می‌خواند (بک‌آپ ۳۰۳، آنالایزر سبز). دو قرعه‌ی
  ۳۰ثانیه‌ای waived شدند؛ سهمیه نسوخت. مجوزهای گوشی ۱۱ سالم‌اند
  (prewarm ok، ~۶۰۰ بسته از پل گذشت) و مسیر رله/TURN روی 192.168.2.1
  فقط از پل می‌گذرد — وای‌فای گوشی نمی‌تواند این ترافیک را دور بزند.
- ۲۰۲۶-۰۸-۰۹ ~۰۹:۵۵ — شات ۱: اپ نصب و اجرا شد (prewarm ok) ولی
  حکم INVALID/UNROUTED با صفر بسته روی en6؛ تست در ثانیه‌ی ۳۰ در
  انتظار اتصال به رله تایم‌اوت شد. تشخیص: مجوز Local Network روی
  گوشی ۱۴ پرومکس هرگز داده نشده — تا Allow نشود هیچ اتصال محلی از
  اپ خارج نمی‌شود. اقدام کاربر: باز کردن اپ/تنظیمات و فعال‌کردن
  Local Network + Allow میکروفن. خط خودکار شات‌های بعدی را می‌زند.


ثبت: ۲۰۲۶-۰۸-۰۹ صبح. این فایل برای ازسرگیری از صفر است: هر کس این را
بخواند بدون تاریخچه می‌داند کجاییم، چطور وصلیم، چه فرمانی بدهد.

## ۰.۲ فاز «تنظیمات و ظاهر» — بک‌لاگ کاربر (شروع ثبت ۲۰۲۶-۰۸-۱۰)

- ۱) عکس در چت — طرح مصوب «تحویل پلکانی» (ثبت ۲۰۲۶-۰۸-۱۰، سطح مدرن):
  الف) انتقال: عکس از مسیر base64ِ متنی خارج شود (۳۷٪+ سربار) و روی
  لاین باینری برود — ARQ در لینک سالم، فواره+دیتاگرام زیر loss>=0.3
  (همان سوییچ اثبات‌شده‌ی ویدیو)؛ آدرس‌دهی محتوایی یعنی dedup و
  resume رایگان (ارسال دوباره‌ی همان عکس = فوری).
  ب) سه پله‌ی نمایش: thumbhash ~۳۰بایتی داخل خود پیام (حباب فوراً با
  پیش‌نمایش تار پر می‌شود، حتی زیر ۶۰٪ اتلاف) ← پیش‌نمایش ~۱۵KB
  (ثانیه‌های اول) ← اصلِ بازکدشده‌ی سقف‌دار (2048px q80؛ اورجینال
  در-صورت-درخواست). حباب پله‌به‌پله ارتقا می‌یابد + نوار پیشرفت.
  ج) UX: انتخاب گالری/دوربین، حباب عکس، وضعیت sha-تأیید.
  د) دروازه‌ی تست: قرارداد پایپ‌لاین سه‌پله روی پورت اتلاف‌دار
  (ترتیب پله‌ها + sha کامل + dedup-resume) در پکیج messaging، سپس
  ردیف ریگ msg-loss60 برای رگرسیون. (تست قرمز قدیمی attach در نشست
  چیپ‌شده در حال فیکس است.)
- ✅ حکم نهایی msg-loss60 (۲۰۲۶-۰۸-۱۰ ~۱۱:۵۴، قرعه‌ی ۴): PASS/STRESS —
  قلم ۱ بخش ۰.۲ کامل بسته شد. عکس حالا فقط پلکانی می‌رود (مسیر base64
  برای عکس حذف؛ voice/file همچنان base64). اعداد رسمی: سه پله به ترتیب
  announced←previewReady←originalVerified؛ اعلان 1ms، پیش‌نمایش 8.1s،
  اصل 13.5s روی فواره+دیتاگرام زیر ۶۰٪ صادقانه؛ سربار نماد 242/96=2.52x
  (کف نظری 2.5x)؛ dedup بازارسال 9.6s بدون payload؛ متن p50=417ms؛
  voice پایدار 195s؛ اتصال تا انتها زنده. جدول: tsv ردیف آخر.
  چهار قرعه خورد؛ دو باگ واقعی وسط راه پیدا و ریشه‌ای فیکس شد:
  (۱) قرعه‌ی ۲: بن‌بست «تأیید پایانیِ» ترنسفرهای کوچک فواره — گیرنده
  کامل می‌شود، تک-DONEاش گم می‌شود، بیکنش خاموش و فرستنده‌ی پارک‌شده
  کر — درمان: solicit در حالت پارک+قحطی STATE (re-HELLO با آهنگ
  stateInterval؛ گیرنده‌ی فعال STATE می‌دهد، کامل‌شده DONE). پچ در
  fountain_stream_transfer.dart + تست پین (پکیج ۷۰/۷۰)؛ پروب مک:
  15KB در 3.5s و 96KB در 11s زیر ۶۰٪ (قبلاً بن‌بست ۳۰s). روی 4MiB
  نامرئی بود چون ۱۲۸ نسل فریم را تا ته جاری نگه می‌دارد.
  (۲) قرعه‌ی ۳: انتظار ۳۰ثانیه‌ای verified کوتاه بود — send مسنجر ack
  را صبر نمی‌کند پس اعلان می‌تواند از بلاب‌ها عقب بماند (انبار orphan
  درست کار می‌کند)؛ پنجره به بودجه‌ی متن بسته شد + خروجی شواهد زنده
  (stagedStages/orphanCount/تحویل اعلان) در MSG_SUMMARY ماند.
  پروب‌های ماندگار مک: staged_small_probe_test.dart و
  staged_full_shape_probe_test.dart (شکل کامل 5b، پنج قرعه).
- وضعیت قلم ۱: تمام — کد، دروازه‌های محلی (پکیج ۷۰/۷۰، اپ ۱۸۳/۱۸۳) و
  ردیف ریگ msg-loss60 سبز. جزئیات پیاده‌سازی:
  - هسته در پکیج messaging: دو فایل نو
    `lib/src/thumb_hash.dart` (کدک DCT ~۳۰بایتی، هر دو سر در یک فایل،
    پین با تست رفت‌وبرگشت) و `lib/src/staged_photo_transfer.dart`
    (PhotoAnnouncement با sha کامل + thumbhash داخل پیام؛
    StagedPhotoSender/Receiver با دو لاین ARQ/فواره؛ آدرس‌دهی محتوایی =
    dedup/resume مجانی؛ ترتیب پله قرارداد است). تست پکیج ۶۹/۶۹ سبز؛
    دروازه‌های CONTRACT: ترتیب سه پله زیر ۳۰٪ اتلاف، sha کامل، dedup
    (بازارسال بدون payload)، فواره زیر ۶۰٪ اتلاف، orphan-race، تک‌بلاب.
  - UX اپ: دکمه‌ی عکس + شیت گالری/دوربین (image_picker + fallback
    file_selector روی دسکتاپ)، ingest با قرارداد 2048px q80 + پیش‌نمایش
    ~15KB + thumbhash (`photo_ingest.dart`)، حباب پلکانی با ارتقای
    rung-به-rung و نشان sha (`chat_screen.dart`)، سیم‌کشی کنترلر با لاین
    لوپ‌بک دوم. کلید NSPhotoLibraryUsageDescription اضافه شد. تست اپ
    ۱۸۳/۱۸۳ سبز؛ تست قرمز قدیمی attach هم فیکس شد (سر peer باید ack
    بدهد — قرارداد ack-paced).
  - تست ریگ: `messaging_survival_test.dart` بخش 5b — اعلان روی مسیر
    متنی، پیش‌نمایش+اصل روی لاین؛ زیر loss>=0.3 فواره+DatagramLanePort
    (اتاق مجزا با کلید `callId#photo` روی رله‌ی 3737)، وگرنه کانال
    negotiated جدا (id 9). سه assert نو: ترتیب پله، sha کامل، dedup.
    Timeout فایل ۳۰→۴۵ دقیقه. کلیدهای نو در MSG_SUMMARY با پیشوند
    staged*؛ photoIntact قدیمی دست‌نخورده تا دروازه‌ی h2_run سازگار بماند.
  - درس ثبت‌شده: id محتوایی در هر دو نگاشتِ فرستنده/گیرنده هست — lookup
    حباب باید جهت-قفل باشد (knowledge tree، discipline).
- ۲) ایمیج در جدول گزارش HTML ردیف مستقل ندارد (حکمش داخل ردیف‌های
  messaging_survival است) ← در report_matrix.py بخش «photo staged»
  جدا شود: سه پله + زمان اصل + سربار از MSG_SUMMARY استخراج و رنگی.

## پلن جهشی «هوشمندی v4 — موجود خودآموزِ بسته‌مدار» (ویژن ثبت ۲۰۲۶-۰۸-۱۰ شب؛ پیش‌نیاز: دروازه‌های v3 سبز)

### وضعیت اجرا — ۲۰۲۶-۰۸-۱۱ (طراحی و شواهد: docs/DESIGN_intelligence_v4.md)

- هر شش ستون پیاده و سبز؛ دروازه‌های اصلی با عدد:
  - GATE LOOP OK — ۲۶ سوئیت سبز (دو fake بی‌گترِ ازپیش‌شکسته رفع شد؛ بک‌آپ ۳۴۹/۳۵۰).
  - بازپخش v4 روی همان کورپوس: کل ۱٫۱۰۷ در برابر ۱٫۰۸۵ v3؛ کالیبراتور ۰٫۹۵۹ ← ۱٫۰۳۶/۱٫۰۳۹/۱٫۰۴۴/۱٫۰۴۴ (هر epoch ≥ ۱٫۰۰)؛ lane/atlas/trend بیت‌به‌بیت بی‌تغییر.
  - ردیف ریگ اقدام پیش‌دستانه: extreme PASS/STRESS ثبت 2026-08-11T10:33:47Z —
    preventedFreezes=58 (پنجره‌ی کامل ۳۰s، صفر قطعی در ۴۵۸s) در برابر مرجع همان
    پروفایل که وسط ران بازیابی ۷.۵s خورده بود؛ sentinelWarns=63 با دلیل عددی.
  - sentinelLeadMs هنوز null — دلیل صادقانه: در ران‌های امروز هیچ فریزی رخ نداد که
    پیش‌بینی بخواهد جلوتر از آن باشد؛ اولین ران فریزدار عددش را می‌دهد.
- کورپوس فدرال شد: h2_run.sh هر ران را خودکار ثبت می‌کند؛ ۳۰ ران (۵ ران TREND-دار
  با ۴ تا ۴۵۸ نمونه/ثانیه). هدف ۱۰۰+ همچنان باز (چند-شب).
- آموزش slipSlope/floor منتظر داده‌ی TREND است — اندازه‌گیری ۲۰۲۶-۰۸-۱۱: روی
  پراکسی بسته‌شمار، ۷۲ ترکیب پارامتر همگی raw=0.5735 (اهرم مرده).
- پیش‌نیاز حمل‌شده از v2 (پراب واقعی resolver + ماندگاری مغزها در دو لانچ) هنوز باز.

چرا v4 و چرا این شکل: هر ستونش پاسخ مستقیم یک ضعف اندازه‌گیری‌شده‌ی v3
است، نه آرزو. اعداد مرجع v3 (بنچمارک بازپخش، ۵ epoch): امتیاز کل ۱٫۰۸۵؛
اطلس ۱٫۳۱؛ لاین ۱٫۰۷؛ پیش‌بین ثابت ۰٫۵۷ (آفلاین)؛ کالیبراتور ۰٫۹۵ (افت —
سلول دووجهی loss60 با نسبت‌های تا ۷۰x که در clamp=4x له می‌شوند).

فرمان شروع چت جدید:

```
RIG_GUIDE بخش «هوشمندی v4» را بخوان و کامل اجرا کن — شش ستون، دروازه: GATE LOOP OK + بازپخش v4 >= v3 روی همان کورپوس + یک ردیف ریگ با اقدام پیش‌دستانه‌ی سنجیده
```

- ستون ۱ — کالیبراتور توزیعی (درمان تنها مؤلفه‌ی افت‌کرده): به‌جای
  EWMAِ اسکالر، چندک‌نگارِ هر سلول (هیستوگرام ثابت-بین یا P²) که کل
  توزیع نسبت واقعی/پیش‌بینی را نگه می‌دارد؛ بودجه = چندکِ انتخابی
  (پیش‌فرض p80). دروازه‌ی مکانیکی: روی همان کورپوس v3، مؤلفه‌ی
  کالیبراتور از ۰٫۹۵ به ۱٫۰۰+ برسد و هیچ مؤلفه‌ی دیگری پایین نیاید.
- ستون ۲ — پیش‌بینِ بسته‌مدار: ۰٫۵۷ِ آفلاین باید زنده شود — روی ریگ،
  شلیک failingSoon اقدام واقعی بزند (کف بقا / پیش‌گرمی لاین /
  آماده‌سازی ویس‌نوت) و SLA/VID_SUMMARY دو فیلد نو بگیرد:
  sentinelLeadMs و preventedFreezes. دروازه: حداقل یک ردیف رسمی که
  lead-time مثبتِ اندازه‌گیری‌شده و اثرش (freeze کمتر نسبت به ران
  مرجع همان پروفایل) ثبت شده باشد.
- ستون ۳ — قهرمان/چالش‌گرِ روی-دستگاه: بنچمارک بازپخش از ابزار توسعه
  به حلقه‌ی دائمی خود اپ ارتقا یابد — اپ شبانه (فقط روی برق/باتری
  بالا) روی call_history و journal خودش epoch می‌زند؛ مغز جدید فقط
  وقتی PROMOTE می‌شود که امتیاز بالا برود، وگرنه rollback (نسل قبلی
  دست‌نخورده روی دیسک). تستِ پین: promote-on-rise و rollback-on-drop.
- ستون ۴ — اطلس پیش‌نگر: از «ساعتِ روز» به «ساعتِ هفته» + مدل گذار
  شبکه‌به‌شبکه (بعد از این وای‌فای معمولاً کدام شبکه می‌آید؟) تا
  پیش‌گرمی قبل از ترک شبکه ممکن شود. تماماً محلی، فقط هش — مثل v3.
- ستون ۵ — راویِ پاسخ‌گو: از روایت یک‌طرفه به پرسش‌وپاسخِ شواهد-محور:
  سه intent قالبی «چرا کند شد؟ / امروز چه یاد گرفتی؟ / چه عوض شد؟»
  که پاسخ هر کدام از journal و call_history با ارجاع [mN] ساخته
  می‌شود — قطعی و قالبی، بدون هیچ LLM. گلدن‌تست هر سه.
- ستون ۶ — کورپوس فدرال ریگ (پایان تبخیر شواهد): h2_run.sh برداشت
  هر ران را مستقیم داخل repo بنویسد (build_replay_corpus.py دیگر
  نجات‌دهنده نباشد، ثبت‌کننده‌ی همیشگی باشد)؛ test.log خطِ ماشینی
  TREND per-second بگیرد تا مؤلفه‌ی پیش‌بین trainable شود و دو ثابت
  slipSlope/floor از داده یاد گرفته شوند (ثابت→مدل). هدف: کورپوس
  ۱۰۰+ ران قبل از بستن v4.
- پیش‌نیاز حمل‌شده از v2: پروب‌های واقعی resolver
  (connectivity_plus / network_info_plus پشت seam موجود
  device_bindings) تا هویت اطلس روی دستگاه واقعی شود؛ اعتبارسنجی
  ماندگاری مغزها روی Documents در دو لانچ متوالی روی گوشی.
- دروازه‌ها: GATE LOOP OK؛ بازپخش v4 روی همان کورپوس v3 ≥ ۱٫۰۸۵ و
  کالیبراتور ≥ ۱٫۰۰؛ ردیف ریگِ اقدام پیش‌دستانه با lead-time ثبتی؛
  تست promote/rollback؛ کورپوس ۱۰۰+؛ ران روی گوشی؛ هیچ صفتی بدون عدد.

## پلن جهشی «هوشمندی v3 — دانشمندِ شبکه‌ی خودی» (ویژن مصوب ۲۰۲۶-۰۸-۱۰؛ حاکم بر v2، پیش‌نیازش تسک ۱ همان v2)

ایده‌ی مرکزی: اپ تنها «تحمل‌کننده‌ی» شبکه‌ی بد نماند — خودش
دانشمندِ شبکه‌ی کاربر شود: اندازه بگیرد، به‌خاطر بسپارد، پیش‌بینی
کند، پیش‌دستانه عمل کند، و هر تصمیمش را به فارسی توضیح دهد. تماماً
روی-دستگاه، بدون هیچ مدل/سرویس خارجی، توضیح‌پذیر و سنجش‌پذیر.

فرمان شروع چت جدید:

```
RIG_GUIDE بخش «هوشمندی v3» را بخوان و کامل اجرا کن — پنج ستون + بنچمارک بازپخش، دروازه: GATE LOOP OK + intelligence_score در گزارش
```

- ستون ۱ — اطلس شبکه (حافظه‌ی مکان-شبکه): به ازای هر هویت شبکه
  (هش SSID/اپراتور)، توزیع سنجیده‌ی loss/rtt/نرخ به تفکیک ساعت در
  DiskJsonStorage؛ قبل از هر تماس، فیزیکِ احتمالی این شبکه معلوم است.
  حریم: همه‌چیز محلی، فقط هش.
- ستون ۲ — پیش‌بینِ خرابی (TrendSentinel v2): از گرادیان‌های زنده
  (شیب loss، تورم rtt، افت نرخ تحویل) قطعیِ نزدیک را جلوتر ببیند و
  پیش‌دستانه عمل کند: پله‌ی نردبان را زودتر پایین بیاورد، لاین
  فواره را پیش‌گرم کند، حالت ویس‌نوت را آماده کند. سنجه: lead-time
  پیش‌بینی در برابر رویداد واقعی روی بازپخش لاگ‌های ریگ.
- ستون ۳ — انتخاب‌گر تجربی لِین (planner همراه با bandit توضیح‌پذیر):
  DeliveryPlanner v2 با Thompson/UCB زمینه‌ای روی lane×شرایط، تغذیه
  از DeliveryOutcomeهای واقعی — آستانه‌ی ثابت loss>=0.3 جایش را به
  تصمیم یادگرفته می‌دهد (fallback قطعی می‌ماند)؛ هر تصمیم رکورد
  «چرا» دارد.
- ستون ۴ — کالیبراسیون شخصیِ بودجه‌ها: ضرایب تصحیح یادگرفته از
  تاریخچه‌ی call_memory روی مدل بودجه‌ی اتصال (فلسفه‌ی فیزیک صادق
  ریگ، این‌بار شخصی و زنده).
- ستون ۵ — روایت‌گر شفاف: دستیار هر بینش/اقدام را به فارسی ساده
  می‌گوید و هر جمله به شناسه‌ی اندازه‌گیریِ ذخیره‌شده ارجاع دارد —
  همان DNA صداقت: هیچ جمله‌ی بی‌سنجه.
- بنچمارک بازپخش (سنجه‌ی «بیشترین امتیاز» که واقعی می‌شود): هارنس
  eval که لاگ‌ها/pcapهای موجود ریگ را آفلاین به مغزها می‌دهد و
  کیفیت پیش‌بینی/تصمیم را در برابر حقیقت زمینی امتیاز می‌دهد →
  `intelligence_score` در report_matrix؛ هر تغییر آینده‌ی مغزها با
  همین عدد سنجیده می‌شود.
- ماتریس سیم‌کشی کامل (هیچ سرویسی بیرون نماند — هر یک هم خوراک
  می‌دهد هم تصمیم می‌گیرد، با تازه‌ترین دستاورد هر کدام):
  · تماس زنده: connect/recovery/نردبان تطبیقی + بودجه‌های مشتق —
    outcome هر تماس به اطلس و کالیبراتور؛ پیش‌بین، پله و پیش‌گرمی
    را فرمان می‌دهد.
  · متن: outcomeهای outbox (تأخیر ack، تکرارها) → یادگیری backoff.
  · ویس‌نوت: صف ماندگار/DTN و حالت degraded → آستانه‌ی ورود/خروج
    حالت را bandit یاد بگیرد.
  · عکس: پایپ‌لاین پلکانی سه‌پله (thumbhash/پیش‌نمایش/اصل) →
    زمان‌بندی پله‌ها و انتخاب لاین از planner؛ سربار واقعی خوراک.
  · ویدیو: دو لاین ARQ/فواره+دیتاگرام با همه‌ی سنجه‌های امروز
    (پروب RTT، سربار 2.5x، rateCap) → انتخاب و پارامترها از planner.
- دروازه‌ی «زنده‌بودن» (خواسته‌ی صریح کاربر — خود-جهشی سنجش‌پذیر):
  بنچمارک بازپخش چند-epoch اجرا می‌شود و بعد از هر epoch مغزها
  ذخیره می‌شوند؛ تستِ منحنی یادگیری پین می‌کند که امتیاز نسبت به
  پایه‌ی نرمال‌شده ۱٫۰ در epochهای اول اکیداً صعودی است
  (مثلاً ۱٫۰ ← ۱٫x ← ۱٫y؛ فلات مجاز، افت = قرمز) — یعنی موجود واقعاً
  از تجربه قوی‌تر می‌شود، نه فقط سبز است. خروجیِ هر نسل = فایل‌های
  مغز (JSON ماندگار experience/learner + گزارش epochها در
  `tools/t2/intelligence_evolution.json`) که آدم‌خوانا بازرسی می‌شود:
  «چه یاد گرفت، چه آستانه‌هایی عوض شد، چرا».
- دروازه‌ها: GATE LOOP OK؛ تست واحد هر ستون؛ بنچمارک بازپخش با
  امتیاز ثبت‌شده + تست منحنی صعودی؛ بازرسی فایل نسل؛ ران روی ۱۴؛
  هیچ صفتی بدون عدد.

## پلن مصوب «هوشمندی v2» (ثبت ۲۰۲۶-۰۸-۱۰ — تسک ۱اش پیش‌نیاز v3؛ تسک‌های ۲-۴ در v3 ادغام شد)

وضعیت فعلی لایه‌ی AI: ساخته و تست‌سبز ولی دِمو-بسته — IntelligenceHub
با دو مغز ماندگار، ConnectionFabric/DeliveryPlanner/MicroLearner،
دستیار RuleBased + seam مدل Gemma (`device_bindings.dart` همه null)،
ForesightCard/AssistantView، call_memory؛ سند کاری binding از قبل:
`docs/BRIEF_intelligence_device_binding.md`. از جهش‌های اخیر
(فواره+دیتاگرام، عکس پلکانی، مانیتور بیس‌لاین، UI شبکه-حقیقت) بی‌خبر.

فرمان شروع چت جدید:

```
RIG_GUIDE بخش «هوشمندی v2» + docs/BRIEF_intelligence_device_binding.md را بخوان و کامل اجرا کن — چهار تسک، دروازه: GATE LOOP OK + ران روی گوشی
```

- تصمیم حاکم (کاربر، جدیدتر از BRIEF قدیمی): هوش مصنوعی اپ کاملاً
  «هوشِ خودی» است — مغزهای یادگیرنده‌ی خودِ محصول (MicroLearner +
  TrendSentinel + قواعد قطعی) — نه Gemma و نه هیچ LLM خارجی. مسیر
  Gemma در BRIEF منسوخ است؛ seam مدل (`GemmaLlmEngine`و
  `LlmBackedAssistant`) بایگانی/حذف شود و معماری‌گارد پلاگین-فری بماند.
- تسک ۱ — binding واقعی «بدون مدل»: probeهای واقعی resolver
  (connectivity/network_info پشت seam)، ماندگاری واقعی مغزها روی
  دیسک دستگاه، و ارتقای RuleBasedAssistant به «هوش خودی v2»: قواعدش
  از سیگنال‌های سنجیده تغذیه شود و آستانه‌هایش را MicroLearner از
  خروجی‌های واقعی یاد بگیرد — قطعی، توضیح‌پذیر، بدون وابستگی مدل.
- تسک ۲ — آموزش مغز اتصال به لاین‌های نو: ثبت لاین فواره+دیتاگرام
  به‌عنوان LaneKind با پروفایل اندازه‌گیری‌شده (مصون از اتلاف، سربار
  ~2.5x)؛ DeliveryPlanner انتخاب عکس/کلیپ را از شرایط سنجیده بگیرد و
  MicroLearner از DeliveryOutcomeهای واقعی پله‌های عکس/ویدیو یاد
  بگیرد — سوییچ ثابتِ loss>=0.3 به تصمیم یادگیرنده ارتقا یابد
  (fallback ثابت بماند).
- تسک ۳ — دستیارِ شبکه-حقیقت: advisory از سیگنال‌های واقعی (مانیتور
  بیس‌لاین، پله‌ی نردبان، خروجی تحویل‌ها) با اقدام پیشنهادی
  (مثلاً سوییچ پیش‌دستانه به ویس‌نوت)؛ ForesightCard در صفحه‌های
  جدید چت/تماس سوار شود (UI v2 آماده است).
- تسک ۴ — حافظه‌ی تماس v2: کارت خلاصه‌ی پس‌ازتماس از آمار سنجیده
  (زمان اتصال، قطعی/بازیابی‌ها، خط زمانی کیفیت) در DiskJsonStorage +
  نمایش در پنل تشخیص — خوراک مستقیم دوسیه.
- دروازه‌ها: `bash tools/run_gate_loop.sh` → GATE LOOP OK؛ تست‌های
  نو برای planner/lane و advisory؛ گلدن دستیار؛ ران روی ۱۴ با و بدون
  فایل مدل؛ هیچ ادعایی بدون سنجه.

## پلن مصوب فاز UI/UX (فوق‌مدرن، ثبت ۲۰۲۶-۰۸-۱۰ — اجرای کامل در چت نو)

فرمان شروع چت جدید:

```
فایل RIG_GUIDE.md بخش «پلن مصوب فاز UI/UX» را بخوان و کامل اجرا کن — دیزاین‌سیستم + پنج صفحه + امضاهای شبکه-حقیقت، با دروازه‌های گلدن/ویجت و ران روی گوشی
```

- بنیاد — دیزاین‌سیستم توکنی (`apps/reference_app/lib/src/ui/tokens.dart`):
  رنگ/تایپ/فاصله/شعاع/موشن به‌صورت توکن؛ لایت/دارک خودکار؛ Dynamic
  Type؛ فارسی+RTL درجه‌یک؛ هپتیک‌های استاندارد؛ بودجه‌ی ۶۰fps
  (const، RepaintBoundary، صفر jank در اسکرول).
- پنج صفحه: فهرست گفتگوها (skeleton loader + حالت خالی) · رشته‌ی چت ·
  تماس زنده · تماس ورودی · تنظیمات (+پنل تشخیص شبکه).
- امضای متمایز = «شبکه-حقیقت» (چیزی که هیچ پیام‌رسانی ندارد و ما
  زیرساختش را اثبات کرده‌ایم):
  · وضعیت هر پیام از ack واقعی: در صف ← رفت ← رسید ← sha-تأیید (نشان).
  · حباب عکس پلکانی: شیمر ← تار thumbhash ← شفاف‌شدن نرم ← نشان sha.
  · ویس‌نوت: ضبط با waveform زنده + slide-to-cancel + پخش با scrub.
  · صفحه‌ی تماس: عقربه‌ی کیفیت زنده از RTCStats واقعی + نمایش پله‌ی
    نردبان تطبیقی (HD←SD←صوت←کف بقا) + بنر خودکار حالت ویس‌نوت.
  · پنل تشخیص در تنظیمات: rtt/loss/نرخ زنده — ویترین فنی برای ارائه.
- دروازه‌ها (هیچ ادعایی بدون این‌ها): ویجت‌تست هر کامپوننت + گلدن‌تست
  لایت/دارک/RTL؛ ۱۸۳+ تست فعلی سبز بماند؛ ران دیدنی روی گوشی ۱۴ و
  اسکرین‌رکورد برای دوسیه؛ سازگاری کامل با قراردادهای موجود
  (ChatDemoController، MSG_SUMMARY، phaseها).

### ✅ حکم اجرای فاز UI/UX (۲۰۲۶-۰۸-۱۰ ~۱۵:۵۰)

- دروازه‌ی تست: suite کامل اپ ۲۶۷/۲۶۷ سبز (پایه ۱۸۵ + ۸۲ تست نو؛ صفر
  رگرسیون)؛ آنالایزر صفر خطا. گلدن‌ها: ۲۲ فایل لایت/دارک/RTL در
  test/ui/goldens (پنج صفحه + گیج + پلیر ویس + پنل تشخیص).
- دیزاین‌سیستم توکنی: lib/src/ui/tokens.dart (رنگ سمانتیک ThemeExtension،
  فاصله/شعاع/موشن const، هپتیک seam، تم کامل M3 لایت/دارک)؛ theme.dart
  شیم سازگاری. الگوی کلیدی: AppMotion.ambientEnabled — هر انیمیشن
  تکرارشونده زیر flutter test اصلاً ساخته نمی‌شود (pumpAndSettle امن؛
  ثبت در درخت دانش best-practices/flutter-ui).
- پنج صفحه: conversations_screen (اسکلتون+خالی+identicon+زمان نسبی) ·
  chat_screen ارتقا (حباب توکنی جهت-آگاه، تیک‌های حقیقت از ack واقعی،
  عکس پلکانی با cross-fade، پلیر ویس با scrub seam) · call_screen ارتقا
  (گیج RTCStats + نردبان HD/SD/Audio/Survival با نام دقیق رونگ + بنر
  ویس‌نوت) · incoming_call_screen (حلقه‌های پالس ambient-gated،
  چیپ callId واقعی CSPRNG) · settings_screen (+ پنل تشخیص زنده با
  برچسب صادقانه‌ی منبع: «synthetic demo profile»).
- شل نو در main.dart: سه تب Call/Chat/Settings؛ فهرست گفتگو ← رشته‌ی
  چت (پوش)؛ پیش‌نمایش تماس ورودی از اپ‌بار؛ ThemeMode بالابرده به MyApp؛
  رونگ از OperatingLadder واقعی روی فید برچسب‌خورده. sendVoiceNote نو در
  ChatDemoController از خط لوله‌ی واقعی اتچمنت (محتوای صوت placeholder
  اعلام‌شده). تست اسموک widget_test با اعلام صریح برای IA جدید اصلاح شد
  (بک‌آپ ۳۴۷)؛ بک‌آپ‌ها ۳۴۰-۳۴۸.
- ران روی گوشی: بیلد ریلیز Runner.app (35.2MB) روی آیفون ۱۴ پرومکس
  (00008120) نصب و لانچ شد (devicectl: «Launched application»).
- ⏳ بلاکر تاریخ‌دار (۲۰۲۶-۰۸-۱۰): اسکرین‌رکورد دوسیه — devicectl برای
  دستگاه فیزیکی capture ندارد (فقط شبیه‌ساز)؛ اسلات: ضبط دستی با
  QuickTime (New Movie Recording ← iPhone) یا Control Center روی خود
  گوشی در اولین نشست حضوری؛ ۲۲ گلدن + اپ نصب‌شده تا آن لحظه دوسیه‌اند.
- سیم واقعی میکروفن/RTCStats دستگاه: از قبل بلاکر فاز است (recorder
  dependency + سیم devConnect) — seamها آماده‌اند و UI هر Stream واقعی
  را همان‌طور رندر می‌کند؛ هیچ داده‌ای در ویجت‌ها اختراع نمی‌شود.

## ۰.۳ پلن مصوب گزینه‌ی الف — لاین فواره‌ای روی ترنسپورت DATAGRAM بدون CC (ثبت ۲۰۲۶-۰۸-۱۰؛ جایگزین ۰.۴ برای خانه‌ی آخر)

چرا: دو ران رسمی ثابت کرد لاین فواره‌ای سالم است ولی SCTP حتی بدون
بازارسال، کل انجمن را زیر ۶۰٪ اتلاف RTO-کلاک می‌کند (~۲ بسته/ثانیه).
درمان ریشه‌ای: نمادهای فواره روی مسیر UDPِ خالص — همان کلاس مسیری که
صوت loss60 رویش PASS شد. هیچ CCِ اتلاف-محوری زیر لاین نمی‌ماند؛
نرخ‌دهیِ خودِ لاین (تحویل-محور + سقف سخت + دریچه‌ی بافر) تنها حاکم است.

فرمان شروع (در همان چت یا چت نو):

```
RIG_GUIDE بخش ۰.۳ را اجرا کن: لاین فواره‌ای را روی ترنسپورت datagram سوار کن و ران رسمی ویدیو loss60 را بزن
```

قدم ۱ — رله‌ی دیتاگرام سمت مک (زیرساخت ریگ، آینه‌ی نقش TURN برای مدیا):
- فایل نو `server/signaling_server/bin/datagram_relay.dart` (~۸۰ خط):
  RawDatagramSocket روی پورت 3479؛ اتاق دونفره با کلید ۱۶بایتیِ اول
  هر دیتاگرام (همان transferId لاین)؛ هر بسته را به «آدرسِ دیگرِ»
  همان کلید فوروارد می‌کند (آدرس‌ها را از مبدأ بسته‌ها یاد می‌گیرد،
  مثل صندلی‌های رله‌ی سیگنالینگ)؛ هیچ حالت دیگری ندارد — لوله‌ی گنگ.
- h2_run.sh آن را کنار relay/turnserver بالا بیاورد و بکشد (الگوی
  موجود در همان فایل) و پورت 3479 به مجموعه‌ی شکل‌دهی و شمارنده‌ی
  UNROUTED اضافه شود — وگرنه متریک ناشکل‌گرفته و حکم دروغ می‌شود
  (همان قانون «مسیر ناسنجیده = REFUSED»).

قدم ۲ — پورت دیتاگرام سمت اپ:
- فایل نو `apps/reference_app/integration_test/support/datagram_lane_port.dart`:
  `class DatagramLanePort implements DataChannelPort` روی
  RawDatagramSocket (dart:io)؛ سازنده: آدرس/پورت رله + transferId
  (هر send همان ۱۶بایت را پیشوند می‌کند؛ inbound پیشوند را می‌کَند).
  بدون هیچ منطق اطمینان — لاین فواره خودش ضد اتلاف/تکرار/بی‌ترتیبی
  است. `transportBufferedBytes` را null بده (سوکت UDP صف محلی معنادار
  ندارد) و MTU-امن بمان: symbolBytes=1024 همان تصمیم قبلی.
- چون هر دو استک تست در یک پروسه‌اند، دو پورت با همان transferId به
  همان رله = مسیر phone→Mac→phone که دوبار از پل شکل‌گرفته می‌گذرد —
  دقیقاً هم‌فیزیکِ مدیای force-relay.

قدم ۳ — سوییچ در تست ویدیو (video_survival_test.dart):
- زیر `loss >= 0.3`: به‌جای data-channel، جفت DatagramLanePort بساز و
  FountainStreamSender/Receiver همان‌ها که سبزند رویش؛ ARQ برای بقیه‌ی
  پروفایل‌ها دست‌نخورده. VID_SUMMARY فیلدهای موجود + transport=datagram.

قدم ۴ — دروازه‌های تأیید به ترتیب (هیچ‌کدام قابل پرش نیست):
- تست واحد جدید: echo محلی DatagramLanePort (بدون شبکه) سبز.
- پروب دو-پورتِ realِ رله روی مک (dart run، بدون گوشی): تحویل 256KB
  زیر اتلاف شبیه‌سازی‌شده‌ی ۶۰٪ (رله + دو سوکت واقعی UDP لوکال).
- ران رسمی: فرمان قدم ۳ بخش ۰.۴ (همان h2_run با video_survival)؛
  انتظار صادقانه: ~2.5x سربار نماد، تحویل 4MB در بودجه.

تله‌های شناخته که پلن دورشان زده:
- CC پنهان: هیچ لایه‌ی reliable/ordered زیر لاین نماند (نه SCTP، نه
  TCP رله) — فقط UDP خام.
- قطعه‌قطعه‌شدن: سیمبل ۱۰۲۴ بایت = یک دیتاگرام، صفر fragmentation.
- شکل‌دهی: پورت 3479 حتماً داخل پایپ و شمارنده، وگرنه REFUSED دستی.
- STATE گیرنده هم از همین پورت برمی‌گردد (دوطرفه شکل‌گرفته).
- iOS Local Network: مقصد همان آدرس مک قبلی است — مجوز موجود کافی است.

## ۰.۴ نشست بعد — یک واحد کار تا ۲۴/۲۴ (ثبت ۲۰۲۶-۰۸-۱۰ ~۰۵:۰۰؛ قدم‌های سیم‌کشی SCTPاش انجام شد — برای خانه‌ی آخر بخش ۰.۳ حاکم است)

فرمان شروع چت جدید (همین را بده):

```
لاین فواره‌ای را به مسیر ویدیوی ریگ وصل کن و ران رسمی ویدیو loss60 را بزن — طبق RIG_GUIDE بخش ۰.۴
```

وضعیت: لاین فواره‌ای ساخته، بازبینی‌شده و سبز است (فقط سیم‌کشی مانده).

- کدِ آماده: `packages/messaging/lib/src/fountain_stream_transfer.dart`
  (کلاس‌ها: FountainStreamSender / FountainStreamReceiver؛ export شده در
  messaging.dart؛ تست: `test/fountain_stream_transfer_test.dart`؛ کل
  پکیج ۵۵/۵۵ سبز؛ پروب دستی: `dart run tool/fountain_probe.dart`).
- قدم ۱ — کانال نامطمئن: در ساخت کانالِ لاین ویدیو، کانال دوم با
  `ordered:false, maxRetransmits:0` بساز (پیش‌شرط سختِ سند بالای فایل
  لاین). جای ساخت کانال: مسیر data-channel در
  `packages/media_webrtc_flutter/lib/src/flutter_webrtc_peer_connection_port.dart`
  و مصرفش در `apps/reference_app/integration_test/support/e2e_support.dart`.
- قدم ۲ — انتخاب پروفایل-محور در تست ویدیو
  (`apps/reference_app/integration_test/video_survival_test.dart`):
  `e2eShapedConditions.loss >= 0.3` ← لاین فواره‌ای؛ وگرنه لاین ARQ
  (BinaryStreamSender) مثل امروز. VID_SUMMARY همان بماند + دو فیلد
  sentSymbols/totalSourceSymbols برای سربار اندازه‌گیری‌شده.
- قدم ۳ — ران رسمی (توپولوژی فعلی: ۱۴ پرومکس روی کابل):

```
cd $REPO
eval "$(tools/t2/usb_peer.sh)"
sudo -n T2_IFACE=$T2_IFACE T2_PEER=$T2_PEER tools/t2/h2_run.sh loss60 integration_test/video_survival_test.dart $DEVICE
```

  مقادیر آخرین کشف (اگر کابل دست نخورده): bridge100 / 192.168.2.3 /
  00008120-0010795A3613C01E؛ ولی همیشه از usb_peer.sh بگیر.
- سبز شد؟ نمودار بخش ۰ را ۲۴/۲۴ کن، بعد فاز «تنظیمات و ظاهر»، بعد
  فاز پژوهشی بخش ۶.
- معوقه‌های غیرمسدودکننده: بیلد-یک‌باره (xctestrun، تسک ۵)، دو تست
  قدیمی اپ (چیپ‌های جدا: chat attach و degraded clip cadence)،
  دو معوقه‌ی قدیمی بخش ۶.

## ۰.۵ ازسرگیری فوری در چت جدید (نسخه‌ی اثبات‌شده ۲۰۲۶-۰۸-۰۹ ~۱۱:۴۵)

مسیر طلایی — چهار قدم، مستقل از اینکه کدام گوشی روی کابل است
(اسکریپت‌ها DEVICE را خودکار می‌گیرند؛ UDID دیگر هیچ‌جا هاردکد نیست):

- قدم ۱ — وضعیت خط قبلی؛ اگر زنده و روی دستگاه درست است، دست نزن:

```
pgrep -fl "night_orchestrator|sniper.sh|h2_run"
tail -5 $REPO/tools/t2/pipelines/logs/sniper_run.out
```

- قدم ۲ — اگر مرده یا دستگاه/آدرس کهنه را هدف می‌گیرد، پاک‌سازی:

```
pkill -f h2_run.sh; pkill -f "flutter test"; pkill -f signaling_server.dart
```

- قدم ۳ — کشف خودکار لینک و دستگاه (هر دو گوشی، هر دو توپولوژی):

```
cd $REPO
eval "$(tools/t2/usb_peer.sh)"
echo "$T2_IFACE $T2_PEER $DEVICE"
```

  خروجی ۳متغیره یعنی آماده. اگر exit=2 (کابل زنده، IPv4 ندارد — گوشی
  بدون سیم‌کارت): روی مک Internet Sharing فقط با تیک «iPhone USB» روشن
  شود (تیک Wi-Fi هرگز)، وای‌فای گوشی خاموش، دوباره eval. سنجش سلامت:
  پینگ به $T2_PEER باید ~1ms و ۰٪ باشد.

- قدم ۴ — روشن‌کردن خط (DEVICE اجباری است؛ بدون آن اسکریپت‌ها با
  خطای قطعی می‌ایستند — این عمدی است تا شب علیه دستگاه غایب نسوزد):

```
nohup env T2_IFACE="$T2_IFACE" T2_PEER="$T2_PEER" DEVICE="$DEVICE" bash tools/t2/pipelines/night_orchestrator.sh > tools/t2/pipelines/logs/orchestrator.out 2>&1 &
```

- حکم‌ها: `tools/t2/pipelines/logs/` (sniper_run.out، overnight_run.out،
  لاگ هر شات جدا)؛ جدول رسمی `tools/t2/h2_results.tsv`.
- پیش‌نیازهای گوشی (هر دو دستگاه): Trust + Local Network + Microphone
  + Auto-Lock=Never — چک‌لیست کامل در بخش ۴. گوشی ۱۱ هر دو مجوز را دارد.
- دروازه‌ی فقط-سیم داخل h2_run.sh فعال است؛ خط سبزش
  (`wired-gate baseline ok`) باید در لاگ هر شات دیده شود.
- شات‌های صفر-بسته/UNROUTED شمرده نمی‌شوند (waiver تا ۲۰ تکرار).
- تله‌های بسته‌شده که دیگر نباید وقت بگیرند: UDID هاردکد در
  sniper/overnight (حالا پارامتری)، سقف اتصال ۳۰ثانیه در
  loopback_call_test (حالا E2E_CONNECT_BUDGET_S را می‌خواند)، ران کهنه
  با هدف قدیمی (قدم ۲)، مجوز Local Network (امضایش: UNROUTED با صفر
  بسته و تایم‌اوت ۳۰ثانیه).
- نقشه‌ی راه تا پایان، به همین ترتیب: (۱) ۲۴/۲۴ سبز · (۲) فاز تنظیمات
  و ظاهر · (۳) مرحله‌ی آخر = بررسی سند پژوهشیِ یکسان‌سازی رفتاری
  (`docs/TRAFFIC_NORMALIZATION_RESEARCH.md`، جزئیات و قیدش در بخش ۶).
- حکم هر شات در `sniper_run.out`/`overnight_run.out` (اسکرچ‌پد قدیم) یا
  `tools/t2/pipelines/logs/` (ران‌های تازه) می‌نشیند؛ جدول رسمی:
  `tools/t2/h2_results.tsv`.

---

## ۰.۶ چالش‌ها و عبور از آن‌ها (قانون کاربر ۲۰۲۶-۰۸-۰۹: هر نشست این را چک کند)

هر چالشی که وقت یا قرعه سوزاند این‌جا ثبت می‌شود: نشانه ← علت ← درمان.
نشست جدید قبل از هر تشخیص تازه اول این جدول را مرور کند.

- ۲۰۲۶-۰۸-۱۰ ~۰۴:۳۰ — لاین فواره‌ای سیم‌کشی شد و دو ران رسمی loss60
  خورد؛ هر دو FAIL با یک امضا ← کشف ساختاری: فروپاشی SCTP-CC مستقل از
  بازارسال است. سیم‌کشی درست است (کانال unreliable با عبور صفر از فیلتر
  toMap بالادستی، helloAcked، هر دو کانال open، فازها connected) ولی
  usrsctp با اتلاف تصادفی دوطرفه‌ی ۶۰٪ کل انجمن را به ~۲ بسته/ثانیه
  می‌بندد: cwnd روی کف، SACKها هم ۶۰٪ تلف، ساعتِ پیشروی = RTO (کف ۱
  ثانیه). maxRetransmits:0 فقط بازارسال را حذف می‌کند، نه CC را.
  شواهد اندازه‌گیری‌شده: شات ۱ sent=1158/790s با سیمبل 1024B؛ شات ۲
  sent=2814 با سیمبل 512B و دروازه‌ی 64KiB و کف 32KiB/s ولی
  bufMax=605KB و ۸۴٪ خوانش‌ها بالای دروازه — صف واقعاً پر بود و سیم
  ~۲.۲ بسته/ثانیه خالی می‌کرد (pcap همان را می‌گوید). نتیجه: هیچ پارامتر
  سمت‌فرستنده این سقف را باز نمی‌کند؛ 4MiB روی ~2.4KB/s یعنی ≥۷۳ دقیقه
  در بهترین حالت — خارج از هر پنجره‌ی صادقانه. پیام loss60 قبلاً از همین
  ترنسپورت پاس شد چون چت به چند صد بسته نیاز دارد، نه ~۲۰هزار.
  درمانِ پیشنهادی (تصمیم معماری، منتظر تأیید): لاین فواره‌ای درست است
  ولی ترنسپورتش باید datagram بدون CC ضرردار باشد — مسیر media/RTP یا
  فلوی UDP رله‌شده‌ی خود تماس؛ کانال دیتای SCTP برای حجیم زیر اتلاف
  سنگین ساختاراً مرده است. سه فیکس جانبی همین نشست در کد نشست و
  تست‌سبز است: عبور maxRetransmits:0 از پورت فلاتر (تله‌ی toMap
  بالادستی + دو تست پین)، expireAfter سازنده‌ایِ گیرنده‌ی فواره (بن‌بست
  انقضای ۹۰s در برابر ریکاوری ۲۲۰s + دو تست)، دروازه‌ی backpressure
  پارامتری فرستنده. بک‌آپ‌ها ۳۱۸-۳۲۶؛ لاگ‌ها
  video_loss60_fountain_official.out / video_loss60_fountain_shot2.out.

- دو آیفون در ناوگان و UDID هاردکد در اسکریپت‌ها ← هر شات DENIED روی
  دستگاه غایب ← شناسه پارامتری شد؛ DEVICE اجباری، UNKNOWN = خطای قطعی
  (بک‌آپ ۳۰۱-۳۰۲). دیگر هیچ UDID در هیچ اسکریپتی hardcode نیست.
- گوشی بدون سیم‌کارت روی کابل IPv4 ندارد ← ریگ IPv4 می‌خواهد ← دو
  درمان اثبات‌شده: (الف) Internet Sharing فقط روی iPhone USB؛
  (ب) کشف ۱۶:۴۰ — بعد از قطع‌ووصل کابل، گوشی گاهی خودش آدرس
  link-local (169.254.x) می‌سازد و هیچ اشتراکی لازم نیست — این الگوی
  تمیزتر است (بدون NAT/پل). قطع‌ووصل کابل رابط و آدرس را عوض می‌کند —
  همیشه از usb_peer.sh بگیر، حفظ نکن.
- تله‌ی Verify/Trust توسعه‌دهنده: تأیید گواهی به اینترنتِ خود گوشی
  نیاز دارد («An internet connection is required to verify...») —
  گوشیِ بدون سیم‌کارت با NAT پاک‌شده هیچ راهی به اپل ندارد و Trust
  بی‌نهایت «Unable to Verify» می‌ماند ← موقتاً وای‌فای گوشی یا
  اشتراک سالم بده، Verify بزن، بعد قطع کن. حذف اپ = پاک‌شدن اعتماد و
  مجوزها؛ Trust و Allow ها باید از نو داده شوند.
- ران‌های کهنه با هدف قدیمی ← پنج DENIED پشت‌سرهم و ساعت‌ها گم‌شدن ←
  قبل از هر سری: pkill سه‌گانه (بخش ۰.۵ قدم ۲).
- مجوز Local Network داده‌نشده ← امضا: UNROUTED با دقیقاً صفر بسته +
  تایم‌اوت ۳۰ثانیه ← Settings > Privacy > Local Network + Microphone؛
  کلید فقط بعد از اولین تلاش اتصال ظاهر می‌شود.
- سقف اتصال ۳۰ثانیه هاردکد در loopback_call_test ← بودجه‌ی ۴۵۰ثانیه‌ای
  STRESS هرگز اعمال نمی‌شد؛ هر قرعه در ثانیه‌ی ۳۰ می‌مرد با برچسب
  UNROUTED ← تست حالا E2E_CONNECT_BUDGET_S / E2E_STRESS_CONNECT_S را
  می‌خواند (بک‌آپ ۳۰۳).
- سقف پنهان دوم روی بودجه: پیش‌فرض ۵دقیقه‌ای flutter_test هر تست را در
  ۳۰۰ ثانیه می‌کشت و بودجه‌ی ۴۵۵ثانیه‌ای connect باز می‌ماند ←
  انوتیشن @Timeout(45min) سطح فایل مثل تست‌های survival (بک‌آپ ۳۰۸).
  درس: «بودجه دادی؟ همه‌ی سقف‌های بالادستش را هم چک کن» — سقف تست،
  سقف فریم‌ورک، واچ‌داگ ریگ.
- پنج خانه‌ی باز «قرعه» نبودند — شش باگ واقعی بودند (کشف ۲۰۲۶-۰۸-۰۹ با
  شکار چندعاملی + راستی‌آزمایی خصمانه؛ امضای اندازه‌گیری: تخصیص TURN
  موفق ولی صفر بسته روی پورت‌های رله در کل ۴۵۰ ثانیه):
  ۱) کاندیدای ICE زودرس برای همیشه نابود می‌شد (ack قبل از apply +
     بدون صف انتظار) ← صف parked + درین بعد از هر description
     (call_controller).
  ۲) سوکت‌های hedged (فقط از loss≥0.3 فعال — دقیقاً پروفایل‌های قرمز)
     ترتیب فریم را می‌شکنند؛ کاندیدا از offer سبقت می‌گیرد ← همان صف
     مقصد حلش کرد.
  ۳) پچ‌پچ سیگنالینگ recovery را «در حال پیشرفت» نگه می‌داشت و هیچ
     ICE restart تازه‌ای نمی‌زد ← سقف ۲×connectionTimeout از آخرین
     هارد-سیکل + حسابداری policy در هر انقضا. ظریف‌سازی ~۱۴:۴۵ با
     رهگیری زنده: سیکل سختِ بی‌قیدِ تلاش ۱ خودش قاتل تحویل بود (ریست
     وسط offer@201s / answer@263s)؛ قید recency برگشت — سکوت واقعی
     سیکل می‌خورد، تحویل اخیر صبر؛ سقف ۲× سرجایش. طرح نهایی ~۱۵:۱۰
     (چهار آناتومی، صفر ریستِ مفید): اتصال اولیه = یک نسل تا ته بودجه؛
     تأخیر هر لگ سیگنالینگ زیر ۸۴٪ اتلاف ۵۴-۲۶۹ ثانیه اندازه‌گیری شد،
     پس هیچ پنجره‌ی کوتاه‌تر از بودجه قاضی عادلی نیست. فقط علت‌های
     زمان‌محور صبر می‌گیرند؛ شکست واقعی کانال/انجین همچنان بلافاصله
     سیکل می‌خورد (۱۶۲/۱۶۲ سبز).
  ۴) سرریز صندلی، سوکتِ زنده‌ی primary را می‌بست (نه زامبی را) و
     کلاینت کل سه سوکت را می‌کشت ← ارتقای hedge به primary
     (signaling_client).
  ۵) از دست رفتن primary همه‌ی hedgeهای سالم را نابود می‌کرد ← همان
     ارتقا.
  ۶) رله نوشتن روی سوکت زامبی را «تحویل» می‌شمرد و بافر نمی‌کرد ←
     رینگ بازپخش همیشگی با فیلتر هویت، بدون پاک‌سازی در flush
     (relay_server).
  ۸) کوریِ Karn در لاین ویدیو (کشف ~۱۷:۳۰، اولین ران رسمی روی ۱۴):
     تماس در ۸ ثانیه وصل شد ولی زیر ۶۰٪ اتلاف تقریباً هر چانک حداقل
     یک‌بار retransmit می‌شود و Karn هیچ نمونه‌ی RTT تمیزی باقی
     نمی‌گذارد — srtt=0، نرخ 1kbps، ۴ از ۲۵۶ چانک در ۷۹۰ ثانیه ←
     پروب RTT با nonce یکتا (فریم‌های ۶/۷) که هرگز retransmit نمی‌شود
     و فقط هنگام قحطیِ نمونه شلیک می‌شود؛ سه تصحیح لنز دوم: بک‌آف
     بوت‌استرپ تا اولین نمونه‌ی چانکی زنده می‌ماند (پروب هزینه‌ی
     serialization چانک را نمی‌پردازد)، مصرف nonce در برخورد، احترام
     به freeze بازیابی. (بک‌آپ ۳۱۴؛ ۵۱/۵۱ سبز.)
  ۷) بن‌بست اتاق‌های جدا (کشف با لاگ verbose کوترن، ~۱۴:۰۰): سوکتی که
     اولین فریمش هارت‌بیت بود به اتاق «session» سنجاق می‌شد و بعد از
     ارتقای hedge، دو طرف برای همیشه در دو اتاق ناهم‌پوشان می‌ماندند —
     تخصیص‌های موفق، صفر CreatePermission، ۱۷۵۲ بسته بالا/۴۰۰ پایین.
     ← هارت‌بیت حالا callId آخرین envelope واقعی را می‌گیرد؛ تست جدید
     هم پین شد (بک‌آپ ۳۱۰؛ ۹۶/۹۶ سبز). ابزار تشخیص: verbose در
     turn.conf که h2_run از این تاریخ خودش می‌نویسد (بک‌آپ ۳۰۹).
  بک‌آپ‌ها ۳۰۴-۳۰۶؛ تست‌ها: call_core ۱۶۲ · signaling ۹۵ · server ۱۹
  همه سبز؛ یک تست سرور به قرارداد جدیدِ بازپخش به‌روز شد (بک‌آپ ۳۰۷).
- پنجره‌ی فقط-Cancel موقع اجرا + «devicectl launch was denied» در لاگ ←
  Trust توسعه‌دهنده منقضی شده (امضای رایگان اپل = ۷ روز؛ ربطی به هر
  تست ندارد — به ازای گواهی ماندگار است) ← Settings > General >
  VPN & Device Management > Developer App > Trust؛ خط خودش retry
  می‌کند. درمان دائمی: حساب توسعه‌دهنده‌ی پولی (پروفایل یک‌ساله).
- ناسازگاری کالیبراسیون ریگ (کشف ۱۵:۳۰ با آناتومی‌ها): مسیر loopback از
  پایپ شکل‌دهی دوبار می‌گذرد؛ rtt از قبل برای دوگذری مدل شده بود
  (extreme: پایپ 1000ms، تعریف اپ 2000ms) ولی loss خام می‌رفت —
  loss60 عملاً ۸۴٪ سرتاسری بود در حالی که بودجه و برچسب ۶۰٪ می‌گفتند؛
  هر لگ سیگنالینگ ۴۲-۲۶۹ ثانیه طول می‌کشید و زنجیره در بودجه جا
  نمی‌شد ← از این تاریخ پایپ 1-sqrt(1-P) می‌گیرد تا سرتاسری دقیقاً
  برابر برچسب پروفایل شود (خط سبز جدید در لاگ: e2e-honest plr؛
  حالت خام با T2_RAW_PLR=1؛ بک‌آپ ۳۱۲). سبزهای قبلی زیر شرایط
  سخت‌تر گرفته شده‌اند و معتبر می‌مانند.
- Auto-Lock گوشی ← گیر ابدی در Installing and launching ← Never.
- اولین بیلد روی دستگاه جدید ۵-۱۵ دقیقه ← صبر، نه ریست.
- pf reload بعد از net_shape restore پیام «NAT اشتراک افتاد» می‌دهد ←
  مسیر ریگ (گوشی↔مک روی پل) سالم می‌ماند؛ toggle لازم نیست، فقط
  اینترنتِ خود گوشی می‌افتد که ریگ لازمش ندارد.

## ۱. الان کجاییم (جدول سه ماتریس)

```
VIDEO   8 خانه:  clean PASS · normal PASS · latency PASS · loss10 PASS
                 bandwidth PASS · narrow PASS   |   loss60 OPEN · extreme OPEN
VOICE   8 خانه:  7 سبز   |   loss60 OPEN  (آیتم تاریخی ۸)
MSG     8 خانه:  6 سبز   |   loss60 OPEN · extreme OPEN
```

هر شش سبزِ ویدیو با «نسل جدید کد» بازتأیید شده‌اند (۲۰۲۶-۰۸-۰۹).
پنج خانه‌ی باز، همگی فقط به قرعه‌ی اتصال زیر اتلاف سنگین گیرند — کد
محلی همه‌جا سبز است.

## ۲. باید کجا باشیم

- هر ۲۴ خانه سبز؛ سپس فاز «تنظیمات و ظاهر» (خواسته‌ی ثبت‌شده‌ی کاربر).

## ۳. نُه ارتقای نسل که در کد نشسته (همه تست-سبز)

```
1  دروازه‌ی فشار برگشتی مشتق از پنجره (messaging/binary_stream_transfer.dart)
2  قانون Karn + RTO تطبیقی RFC6298 (همان فایل)
3  backoff بوت‌استرپ + کف retransmit مشتق از RTT (تست ویدیو)
4  پنجره‌ی AIMD به‌جای آینه‌ی BDP (همان فایل)
5  سقف درین-محور صف + chunk مشتق از فیزیک لینک (sender + تست ویدیو)
6  قاضی واحد + بدون-صبر-تازه + سقف صبر پیشرفت + EngineTimeoutException
   (call_core/call_controller.dart)
7  مهلت رگبار media_disconnected (همان فایل)
8  مالکیت تک‌نفره‌ی generation در اتصال اولیه (همان فایل)
9  صندلی چند-سوکته در رله (server/relay_server.dart) + سوکت‌های hedged
   در کلاینت (signaling/signaling_client.dart، پارامتر hedgeSockets،
   فعال روی loss>=0.3 در e2e_support.dart)
+  مسیر TCP رله در iceServers + شکل‌دهی صادقانه‌ی همان پورت + شمارنده‌ی
   UNROUTED که TCP-رله را هم شواهد می‌شمارد (tools/t2/h2_run.sh)
```

تست‌ها: call_core ۱۶۲ · media_webrtc ۹۷ · signaling ۹۵ · messaging ۵۱ ·
server ۱۹ — همه سبز. بک‌آپ‌های شماره‌دار تا ۲۹۸ در `.backups/`.

## ۴. توپولوژی — چطور به گوشی وصلیم

### الگوی طلایی (۲۰۲۶-۰۸-۰۹ ظهر): کابل USB + لینک local

اینترنت مک از وای‌فای خانه (en1)؛ داده‌ی تست فقط از کابل USB (en6).
پایدارترین الگو — رادیو حذف، ~۰.۹ms، صفر اتلاف پایه.

```
مک   (en6):   169.254.110.17
گوشی (USB):   169.254.209.150   ← T2_PEER
اجرا: T2_IFACE=en6 T2_PEER=169.254.209.150
دستگاه: 00008120-0010795A3613C01E (آیفون ۱۴ پرومکس)
```

چک‌لیست راه‌اندازی دستگاه جدید روی این الگو (ترتیب مهم است):

- کابل وصل، گوشی آنلاک، Trust This Computer → Trust.
- آی‌پی گوشی روی لینک: `ping` به آدرس 169.254 گوشی باید ~۱ms و ۰٪ باشد.
- اولین بیلد روی دستگاه جدید ۵-۱۵ دقیقه است (کش خالی) — صبر، نه ریست.
- تله‌ی «Unable to Verify App» (دیده‌شده روی ۱۱ پرومکس، ۲۰۲۶-۰۸-۰۹):
  پیام دقیقاً می‌گوید برای تأیید گواهی توسعه‌دهنده اینترنت لازم است.
  اینترنت‌شرینگِ روشن کافی نیست — سنجش همان لحظه نشان داد
  `net.inet.ip.forwarding: 0`، یعنی مک آدرس می‌داد ولی ترافیک را عبور
  نمی‌داد. راه قطعی: وای‌فای گوشی را موقتاً روشن کن و به شبکه‌ی خانه
  وصل شو، بعد Settings ← General ← VPN & Device Management ← روی ردیف
  Apple Development بزن ← Verify App؛ سپس وای‌فای را دوباره خاموش کن
  تا برای ران‌ها فقط کابل بماند.
  علامت اینکه هنوز Trust نشده: اجرای اپ با پیام
  «invalid code signature … profile has not been explicitly trusted».
- Auto-Lock گوشی باید Never باشد (Settings > Display & Brightness >
  Auto-Lock)؛ قفل‌شدن صفحه اپ را معلق می‌کند و ران در مرحله‌ی
  «Installing and launching» برای همیشه گیر می‌کند — امضای این تله.
- مجوزهای گوشی برای اپ reference هر دو باید فعال باشند:
  Settings > Privacy & Security > Local Network > اپ روشن
  Settings > اپ reference > Microphone روشن
  (کلید Local Network فقط بعد از اولین تلاشِ اتصال در فهرست ظاهر می‌شود.)
- امضای «مجوز بسته است»: حکم INVALID/UNROUTED با دقیقاً صفر بسته و
  تایم‌اوت ۳۰ثانیه‌ای تست — حتی یک SYN هم از en6 رد نمی‌شود.
- تله‌ی بزرگ زمان‌خوار: ران‌های کهنه‌ای که هنوز دستگاه یا آدرس قدیمی
  را هدف می‌گیرند (نشانه: `prewarm DENIED`). قبل از هر سری:

```
pkill -f h2_run.sh; pkill -f "flutter test"; pkill -f signaling_server.dart
```

- در اسکریپت‌های خط، شاتِ صفر-بسته از سهمیه کم نمی‌شود (وصله‌ی waiver:
  تا ۲۰ تکرار با فاصله‌ی ۹۰ثانیه تا مجوز/لینک درست شود).

### دروازه‌ی مکانیکی فقط-سیم (نصب‌شده در کد، ۲۰۲۶-۰۸-۰۹)

قانون «رادیو در مسیر تست ممنوع» دیگر توصیه نیست — داخل خود
`tools/t2/h2_run.sh` نشسته و هر ران آینده بدون استثنا از آن رد می‌شود:

- رد قطعی اینترفیس رادیو: اگر T2_IFACE همان کارت وای‌فای مک باشد، ران
  قبل از هر شکل‌دهی با پیام REFUSED می‌میرد.
- سنجش پایه‌ی سیم‌بودن: ۲۰ پینگ بدون شکل‌دهی؛ فقط اتلاف زیر ۵٪ و
  تأخیر میانگین زیر 20ms قبول است (سیم ~۱ms است؛ هر رادیویی رد می‌شود).
- آزمایش عمدی روی رادیو فقط با override آگاهانه:

```
T2_ALLOW_RADIO=1
```

نتیجه: فاجعه‌ی شب ۲۲قرعه‌ای (مرگ یک‌طرفه‌ی AP) از نظر مکانیکی دیگر
قابل تکرار نیست — مسیر ناسالم قبل از سوختن قرعه REFUSED می‌شود.

### گوشی بدون سیم‌کارت (۱۱ پرومکس) — تشخیص و درمان (اندازه‌گیری ۲۰۲۶-۰۸-۰۹)

شواهد اندازه‌گیری‌شده روی همین کابل:

```
IPv6 link-local:  5/5 بسته، 0.0% اتلاف، میانگین 1.136ms   ← کابل سالم است
IPv4 روی کابل:    هیچ همسایه‌ای در جدول ARP               ← گوشی آدرس ندارد
```

علت: آیفونِ بدون سیم‌کارت هیچ Personal Hotspot ندارد، پس iOS رابط
شبکه‌ی USB را بالا می‌آورد ولی هرگز روی آن IPv4 تنظیم نمی‌کند. ریگ با
IPv4 کار می‌کند، پس آدرس باید از سمت مک داده شود.

درمان یک‌باره (کاملاً سیمی، بدون هیچ رادیویی):

- روی مک: System Settings ← General ← Sharing ← Internet Sharing
  · Share your connection from: Wi-Fi
  · To computers using: فقط «iPhone USB» تیک بخورد
  · تیکِ Wi-Fi هرگز زده نشود — همان حالتِ AP بود که شب ۲۰۲۶-۰۸-۰۸
    بیست‌ودو قرعه را کشت.
- روی گوشی ۱۱: وای‌فای را خاموش کن. اگر گوشی هم‌زمان روی وای‌فای خانه
  باشد، ترافیک می‌تواند از مسیر شکل‌نگرفته برود و عدد تمیز با برچسبِ
  شکل‌گرفته تولید کند.
- بعد از روشن‌کردن اشتراک، رابط `bridge100` ساخته می‌شود و گوشی از راه
  DHCP آدرس می‌گیرد؛ کشف خودکار هر دو را برمی‌دارد:

```
eval "$(tools/t2/usb_peer.sh)"
```

- اسکریپت کشف (نسخه‌ی ۶) همین حالت را با نام تشخیص می‌دهد و راه‌حل را
  چاپ می‌کند؛ کدهای خروج: 0 آماده · 2 کابل زنده ولی IPv4 ندارد ·
  1 اصلاً همسایه‌ای نیست.
- تله‌ی ثبت‌شده: رابط وای‌فای هم مسیر 169.254 دارد، پس هر جست‌وجوی
  همسایه باید رادیو را صریحاً کنار بگذارد وگرنه میزبان‌های شبکه‌ی خانه
  را می‌گیرد.

نتیجه‌ی اشتراک روی ۱۱ پرومکس (اندازه‌گیری ۲۰۲۶-۰۸-۰۹ ~۱۱:۳۰):

```
bridge100 = 192.168.2.1 (مک)   عضو: en6
گوشی      = 192.168.2.2        از راه DHCP  ← آدرس گرفت
کیفیت     = 2/20 پاسخ، 90% اتلاف، ولی 0.55ms وقتی پاسخ می‌دهد
```

یعنی آدرس‌دهی حل شد و سیم از نظر فیزیکی عالی است، ولی گوشی در حالت
خواب فقط ~۱۰٪ پاسخ می‌دهد. تا صفحه‌ی گوشی روشن و باز نباشد
(Auto-Lock=Never و اپ در پیش‌زمینه)، دروازه‌ی فقط-سیم این مسیر را
درست REFUSED می‌کند — چون پایه‌ی کمتر از ۵٪ اتلاف را ندارد.
پس قدم اجباری قبل از هر ران: صفحه‌ی گوشی روشن و آنلاک بماند.

### سویچ بین دو گوشی — روش مدرن فقط-USB (ثبت ۲۰۲۶-۰۸-۰۹)

ناوگان دو دستگاه دارد؛ هر وقت ۱۴ پرومکس لازمِ کاربر شد، تست روی ۱۱
پرومکس ادامه می‌یابد. ۱۱ پرومکس سیم‌کارت ندارد و مهم نیست: در الگوی
طلایی، گوشی نه اینترنت می‌خواهد نه هات‌اسپات — کل رگ (رله + TURN +
شکل‌دهی) روی مک است و از کابل رد می‌شود.

```
iPhone 14 Pro Max   00008120-0010795A3613C01E   (iPhone15,3)
iPhone 11 Pro Max   00008030-001215003AF2802E   (iPhone12,5، بدون سیم)
```

روال سویچ (هر دو جهت، بدون هیچ تنظیم دستی آدرس):

- قانون: هر بار فقط یک گوشی روی کابل.
- گوشی هدف را وصل کن، آنلاک، اگر پرسید Trust.
- کشف خودکار همه‌چیز با یک فرمان (اسکریپت در repo، تست‌شده‌ی زنده):

```
cd $REPO
eval "$(tools/t2/usb_peer.sh)"
echo "$T2_IFACE $T2_PEER $DEVICE"
```

- همین سه متغیر را به هر ران بده؛ آدرس link-local هر گوشی فرق دارد و
  hardcode ممنوع:

```
sudo -n T2_IFACE=$T2_IFACE T2_PEER=$T2_PEER tools/t2/h2_run.sh loss60 integration_test/loopback_call_test.dart $DEVICE
```

- بار اول روی هر دستگاه: بیلد ۵-۱۵ دقیقه + دو مجوز (Local Network و
  Microphone) طبق چک‌لیست بالا. ۱۱ پرومکس قبلاً هر دو را دارد.
- سازوکار اسکریپت: جدول ARP جفتِ اینترفیس+آدرس همسایه‌ی 169.254 را
  می‌دهد (ورودی permanent = خود مک، حذف)؛ همسایه‌ای که پینگ بدهد گوشی
  است؛ مدلِ ردیف connected در devicectl به UDID نگاشت می‌شود.

### الگوی قدیم ۱ (بایگانی): هات‌اسپات گوشی

گوشی خودش AP است؛ مک کلاینتِ هات‌اسپاتِ گوشی است از راه وای‌فای (en1).

```
گوشی (AP):       172.20.10.1   ← T2_PEER
مک   (کلاینت):   172.20.10.x   روی en1 (DHCP از گوشی؛ الان .7)
اینترفیس ریگ:    en1           ← باید T2_IFACE=en1 پاس شود
```

- چرا این الگو: رادیوی مک (که هم کلاینت خانه بود هم AP اشتراک اینترنت)
  شب‌ها بازه‌ی سالم پنج‌دقیقه‌ای هم نگه نمی‌داشت — ۲۲ قرعه‌ی شب را
  همین کشت. AP گوشی پایدار است.
- تله‌ی رفع‌شده: آی‌پی دستی روی en6 با ساب‌نت en1 تداخل داشت و مسیر
  فلپ می‌کرد — با این فرمان پاک شد (در صورت تکرار):

```
sudo ifconfig en6 delete 172.20.10.2
```

- آی‌پی گوشی را چطور پیدا کنیم: gateway پیش‌فرض هات‌اسپات همیشه
  `172.20.10.1` است؛ راه عمومی:

```
route get default | grep gateway
```

- سلامت لینک خام (باید ~۰٪ اتلاف باشد؛ جیتر تا ~۲۰۰ms طبیعی هات‌اسپات):

```
ping -c 20 -i 0.3 172.20.10.1
```

### الگوی قدیم (بایگانی): Internet Sharing مک

گوشی به شبکه‌ی اشتراکی مک وصل می‌شد؛ پل `bridge100`، مک `192.168.3.1`،
گوشی `192.168.3.3` (اجاره در `/var/db/dhcpd_leases`؛ مراقب چرخش MAC
خصوصی iOS — دو اجاره‌ای). پیش‌فرض اسکریپت‌های ریگ همین است، پس در
الگوی هات‌اسپات حتماً `T2_IFACE=en1 T2_PEER=172.20.10.1` بدهید.

### اتصال فرمان به گوشی (مستقل از شبکه)

- شناسه‌ی دستگاه برای flutter: `00008120-0010795A3613C01E` (۱۴ پرومکس؛
  قدیمی: `00008030-...` آیفون ۱۱ — دیگر استفاده نشود)
- تانل devicectl (USB/شبکه) جدا از مسیر ریگ است:

```
xcrun devicectl list devices
xcrun devicectl device process launch --device FFEE11A1-786F-54EB-ADB7-38CAC724C4AC com.tlscodes.referenceApp
```

## ۵. چک‌لیست فرمان‌ها

### اجرای یک ردیف منفرد (الگوی طلایی USB — T2_IFACE مهم!)

```
cd $REPO
sudo -n T2_IFACE=en6 T2_PEER=169.254.209.150 tools/t2/h2_run.sh loss60 integration_test/loopback_call_test.dart 00008120-0010795A3613C01E
```

تست‌های دیگر: `video_survival_test.dart` (ویدیو کامل)،
`sla_thresholds_test.dart` (صوت)، `messaging_survival_test.dart` (پیام).
پروفایل‌ها: clean normal latency bandwidth narrow loss10 loss60 extreme.

### خطوط خودکار (کانونی: repo — `tools/t2/pipelines/`)

روش روشن‌کردن = قدم‌های ۳ و ۴ بخش ۰.۵ (کشف خودکار + nohup).
نسخه‌ی اسکرچ‌پدی نشست‌های قبل بایگانی است — دیگر استفاده نشود.

- ترتیب: تک‌تیرهای ویدیو loss60 (فقط-اتصال، ارزان) → با اولین سبز، ران
  کامل ویدیو برای مهر رکورد → extreme → صوت loss60 (۶ قرعه) →
  پیام‌رسانی loss60 و extreme (۴+۴).
- خروجی‌ها: `sniper_run.out` و `overnight_run.out` در
  `tools/t2/pipelines/logs/`؛ جدول رسمی: `tools/t2/h2_results.tsv`؛
  گزارش تصویری: `python3 tools/t2/report_matrix.py` →
  `tools/t2/h2_report.html`.

### پاک‌سازی پس از کرش

```
pkill -f h2_run.sh; sudo -n dnctl list        # خالی = شکل‌دهی تمیز
sudo tools/t2/net_shape.sh restore            # اگر خالی نبود
```

## ۶. کار باقی‌مانده — چک‌لیست دستوری

- [x] خطوط خودکار با کشف خودکار لینک/دستگاه ران شدند (بخش ۰.۵؛ ۲۰۲۶-۰۸-۰۹).
- [ ] ویدیو loss60/extreme — بلوکر طراحی ثبت‌شده ۲۰۲۶-۰۸-۰۹ ~۱۸:۰۰:
      با پروب‌های RTT ثابت شد کنترل ازدحام SCTPِ کانال داده زیر ۶۰٪
      اتلافِ تصادفی به ~1kbps فرومی‌پاشد (srtt=13s، minRtt=3s) — تحویل
      4MB در پنجره از نظر فیزیکی ناممکن است؛ قرعه‌ی بیشتر بی‌معناست.
      معماری مصوب (کاربر ۲۰۲۶-۰۸-۱۰، «جهش واقعی»): حذف کامل ARQ —
      کدگذاری فواره‌ای (RaptorQ-خانواده) روی کانال داده‌ی نامطمئن/
      نامرتب؛ گیرنده با هر K(1+ε) نمادِ دلخواه بازسازی می‌کند، اتلاف
      فقط سربار نسبی است نه توقف؛ نرخ‌دهی BBR-گونه از نرخ-تحویل و
      پروب‌های RTT (نه سیگنال اتلاف)؛ آدرس‌دهی محتوایی/resume و sha256
      حفظ می‌شود؛ پنجره‌ی حکم = تابع ظرفیت×سربار اندازه‌گیری‌شده.
      پیاده‌سازی: نشست بعد، پکیج messaging (لاین جدید کنار لاین فعلی،
      انتخاب پروفایل-محور).
- [ ] صوت loss60 (آیتم ۸) سبز.
- [ ] پیام‌رسانی loss60 و extreme سبز.
- [x] چهار اسکریپت خط داخل repo ماندگار شدند (`tools/t2/pipelines/`).
- [ ] پس از ۲۴/۲۴: فاز «تنظیمات و ظاهر».
- [ ] مرحله‌ی آخرِ نقشه‌ی راه — فاز پژوهشی (ثبت ۲۰۲۶-۰۸-۰۹، خواسته‌ی
      صریح کاربر: بعد از سبزشدن هر ۸ ردیفِ هر سه ماتریس و پس از فاز
      تنظیمات و ظاهر، همین سند در چت آن‌روز باز و بررسی شود).
      مسیر واقعی فایل:

```
$REPO/docs/TRAFFIC_NORMALIZATION_RESEARCH.md
```

      (مسیر `~/src/ruby-3.3.0/docs/...` که گاهی گفته می‌شود وجود ندارد —
      وقت تلف نکنید.)

      موضوع: یکسان‌سازی رفتاری جریان‌ها. بخش ۸ آن سه مؤلفه‌ی موجود
      پروژه را به‌عنوان اجزای آماده نام می‌برد: بودجه‌ی سیم کدک،
      سطل ژتون لاین، و فشار برگشتی بافر. پنج پرسش آماده‌ی مشورت در
      بخش ۱۱ دارد.
      قید سختی که همان سند ثبت کرده و باید رعایت شود: روی لینک‌های
      باریک، صوت دوطرفه با کف کدک هم جا نمی‌شود؛ پس سربار یکسان‌سازی
      آنجا بقای تماس را می‌کشد. نمایه باید تابع پهنای باند اندازه‌گیری‌شده
      باشد و در باریک‌ترین پروفایل‌ها خاموش شود.
      دروازه‌ی شروع: تا وقتی ۲۴/۲۴ سبز نشده، این فاز باز نمی‌شود —
      وگرنه ماتریسِ در حال بسته‌شدن با یک متغیر تازه آلوده می‌شود.
- [ ] معوقه‌ی ابزار (خواسته‌ی کاربر ۲۰۲۶-۰۸-۰۹، «رویکرد مدرن»): بیلد
      یک‌باره + اجرای چندباره برای شات‌ها — الان هر شات flutter test
      کامل می‌زند؛ هدف: build-for-testing یک‌بار به ازای (پروفایل +
      اثر انگشت سورس)، بعد test-without-building با xctestrun در هر
      شات. پیش‌نیاز: تارگت RunnerTests و سازگاری parsing لاگ.
- [ ] معوقه‌ی محصولی: رانگ خودکار «صوت جا نمی‌شود → یک‌طرفه» در
      MediaAdaptationDriver (الان فقط در پیکربندی تست ریگ است).
- [ ] معوقه‌ی ابزار: seam منبع جریانی برای کلیپ کامل ۳۵۰-۴۵۰MB (قدم ۳.۴).

## ۷. تله‌های ثبت‌شده (خلاصه — تفصیل در STATE_AND_NEXT.md)

- ردیف INVALID/UNROUTED یعنی ترافیک اصلاً از مسیر شکل‌گرفته نگذشته —
  تقصیر پروتکل نیست؛ اول سلامت لینک خام را چک کن.
- پروفایل‌های loss ذاتاً قرعه‌اند؛ ابزار درست، قرعه‌های پیاپی است.
- ارورهای `no address on bridge100` یعنی T2_IFACE پاس نشده (الگوی
  هات‌اسپات را با bridge پیش‌فرض صدا زده‌ای).
- USB وصل بودن گوشی ≠ روی شبکه بودن؛ تست شبکه فقط با پینگ.
- هر ویرایش کد: اول بک‌آپ شماره‌دار در `.backups/`، بعد Edit؛ آنالایزر
  و تست پکیجِ لمس‌شده در همان نوبت.
