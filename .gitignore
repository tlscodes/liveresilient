# Dart/Flutter
.dart_tool/
.packages
build/
.pub-cache/
.pub/
pubspec.lock
**/doc/api/
.flutter-plugins
.flutter-plugins-dependencies

# IDE / OS
.idea/
.vscode/
*.iml
.DS_Store

# Local backups / archives
.backups/
*.bak
*.tar.gz

# Generated dev TLS certs (signaling_server)
.dev-certs/

# conversation transcripts / logs — never in the technical repo
chats.md
*.log
# Retained verification output (tools/run_suites.sh, cold_check.sh,
# workspace_gate.sh). Kept on disk so an intermittent failure still has its log
# tomorrow; not committed, because it is evidence of a run, not source.
tools/suite-logs/
models/
__pycache__/
docs/TICKET4_FIRST_RECORD/work/

# Working files of the local helper started by tools/t2/step5_helper.sh: a
# generated key, its config list and a run log. The configuration that matters
# is recorded in docs/evidence/step5_helper_config.txt, which IS committed.
tools/t2/step5_work/

# The full capture behind step 7: 65MB of the attached device every packet,
# including unrelated personal traffic. Its sha256, size and scan transcript are
# recorded in docs/evidence/step7_trace_analysis.txt, which is what the check reads.
docs/evidence/step7_trace_raw.pcap

# Phase 5 / full-test heavy artifacts (rebuildable — never in history)
Archive.zip
tools/phase5/native/
apps/reference_app/ios/NativeCodecs/
