# PLAN_PHASE5_CODEC — فاز ۵: لایه‌ی کدک فوق‌سبک روی جاده‌ی اثبات‌شده

ثبت ۲۰۲۶-۰۸-۱۹. پیش‌نیازِ ورود: ماتریس ترنسپورت 24/24 سبز (h2_results.tsv)
+ لاین RLNC دیتاگرام (سربار 2.54x زیر ۶۰٪ اتلاف، ران رسمی ۳۰۳s).
این فاز «بار سبک روی جاده» است: شش قله، هر یک با گیتِ بایت‌شمارِ مکانیکی.
جدول رسمی این فاز: `tools/phase5/h3_results.tsv`
(ستون‌ها: Feature, Standard_Size, Compressed_Size, Wire_Time_At_2Kbps,
Loss_Survival, Status).

## قواعد صداقت (بالاتر از همه‌ی قله‌ها)

- هر گیت روی «کورپوس بنچمارک ثابتِ داخل repo» اندازه می‌گیرد (نه بدترین
  حالتِ دلخواه): متن‌های نمونه‌ی فارسی/انگلیسی، عکس مرجع، صدای ضبطی مرجع.
  گیت = p50 و p95 روی همان کورپوس؛ عدد worst-case جدا ثبت می‌شود.
- عدد فقط از اسکریپت گیت می‌آید (`tools/phase5/gate_N.sh` → exit 0/1 +
  سطر TSV). ادعای PASS بدون سطر TSV = دروغ سبز.
- جایگزینِ سبک‌تر (مثلاً WebP به‌جای AVIF) فقط با برچسب صادقانه‌ی خودش
  در TSV می‌نشیند؛ «تقریباً همان» ممنوع.
- کیفیت ادراکی (صدای Codec2، فریم AV1) قابل‌گیتِ مکانیکی نیست — برای هر
  قله یک artifact شنیدنی/دیدنی در `tools/phase5/artifacts/` تحویل می‌شود
  و قضاوتش با مالک است؛ گیتِ مکانیکی فقط بایت و decode-پذیری را می‌بندد.

## شش قله، به ترتیب ریسک/وابستگی (نه ترتیب جدول)

### قله ۱ — متن: پیام ≤ ۸۰ بایت (خالص Dart، بدون FFI — شب اول همین‌جا شروع)
- اسکیمای باینری فشرده (protobuf-سبک دستی یا FlatBuffers) + zstd سطح ۱۹
  با دیکشنریِ آموزش‌دیده روی کورپوس چت فارسی/انگلیسی repo (بودجه و
  ورژن‌گذاری دیکشنری: پیوست ج).
- گیت 1: p50 ≤ 80B و p95 ≤ 100B روی کورپوس ۲۰۰پیامی + round-trip بی‌اتلاف.
- خروجی: `packages/messaging/lib/src/compact_text_codec.dart` + گیت + TSV.

### قله ۶ — صفحه‌ی خبر ≤ 1.5KB (خالص Dart)
- CBOR + Brotli؛ ۳۰۰ کلمه + یک تصویر برداری/BlurHash ≤ 500B.
- گیت 6: صفحه‌ی مرجع ≤ 1536B و پارس/رندر round-trip.

### قله ۲ — عکس ≤ 3KB
- پایپ‌لاین resize به 160x120/240x160 + AVIF (libavif) یا WebP خاکستری
  کوانتیزه‌ی تهاجمی (fallback با برچسب خودش).
- گیت 2: عکس مرجع دوربین (۲-۱۰MB) → خروجی ≤ 3072B + decode موفق.

### قله ۳ — پیام صوتی ۱۰ثانیه ≤ 1KB
- Codec2 (حالت 700C یا 1200) با FFI به C — Lyra فقط stretch بعد از سبزی
  Codec2. جایگزینی مسیر ضبط Opus در حالت throttled.
- گیت 3: ۱۰ ثانیه گفتار مرجع → ≤ 1000B + decode + artifact شنیدنی.

### قله ۵ — موتور PTT ≤ 750bps روی سیم
- فریم‌های Codec2 (۲۸ بیت/۴۰ms) روی همان DatagramLanePort (بدون SCTP/TCP)؛
  half-duplex، بدون handshake.
- گیت 5: دو-سطحی طبق پیوست الف — کل بایت سیم (پی‌لود+تگ+UDP/IP) در
  پنجره‌ی ۶۰ثانیه ≤ 750bps + بقا زیر ۶۰٪ اتلاف؛ تجمیع فریم و تگ ۲بایتیِ
  نسخه‌ی دومِ فورواردر ریگ، دو پیش‌نیاز محاسباتیِ همین گیت‌اند (پیوست الف).

### قله ۴ — ویدیو-نوت ۵ثانیه ≤ 15KB (پرریسک‌ترین — آخر)
- SVT-AV1/libaom در 96x64 یا 128x96، ۲-۳fps + صدای Codec2 مونو.
- گیت 4: کلیپ ۵ثانیه‌ی مرجع → ≤ 15360B + decode فریم‌ها؛ فقط استریم
  خام با هدر ≤ ۱۶B (پیوست ب) — MP4/MKV/WebM مردود.

## گیت نهایی فاز (goal_verify_cmd)

```
bash tools/phase5/goal_verify.sh
# سبز فقط وقتی: h3_results.tsv هر شش ردیف را با Status=PASS (یا PASS با
# برچسب fallback مصوب) دارد و هر گیت جداگانه exit 0 می‌دهد.
```

## اسکلت run.json (برای آرم AUTORUN — گاردهای A تا D فعال‌اند)

- الگوی هر قله (سه قدم): build → gate → tsv-row؛ verify_cmd فقط
  چک‌کننده‌ی مصنوع (exit-0)، هرگز خودِ اجرای طولانی.
- `arm_next` فقط روی قدم‌هایی که کار بعدی‌شان بیلد/دانلود طولانی است
  (فرمانِ فقط-خواندنی: pgrep یا test -f روی مارکر/لاگ) — قدم آخر هرگز.
- سطح ران: `"goal_verify_cmd": "bash tools/phase5/goal_verify.sh"`.
- FFI/برداشتن dependency (قله‌های ۲،۳،۴): نصب‌ها (brew/pub) در APPROVE
  هنگام آرم؛ هیچ sudo تازه‌ای لازم نیست (ریگ h2 برای این فاز اجباری
  نیست؛ e2e روی دستگاه = فاز بعدی، جدا).
- هر نشست در worktree خودش (`ns-worktree.sh phase5`)؛ یک AUTORUN per cwd.

## پیوست الزامات فنی (چهار تله‌ی پنهان — بدون این‌ها گیت‌ها در اجرا می‌سوزند)

### الف. قله ۵ — آناتومی بایتِ هر پکت، نه فقط نرخ کدک

فریم Codec2 700C = ۲۸ بیت در ۴۰ms (۳.۵ بایت). هدرها غالب‌اند؛ ریاضی
واقعی روی مسیر فعلی (UDP/IP ~28B + کلید اتاق 16B در هر دیتاگرام):

```
1 frame/packet:  (28+16+3.5)   /0.04s  ->  ~9500 bps   (13x بودجه)
1s bundling:     (28+16+87.5)  /1s     ->  ~1052 bps
4s bundling:     (28+16+350)   /4s     ->   ~788 bps    هنوز قرمز
4s بدون کلید ۱۶بایتی: (28+350) /4s     ->   ~756 bps    مرزی
```

پس گیت ۵ سه اهرم دارد که پلن صریح می‌کند:
- تجمیع فریم (Frame Bundling) ۱ تا ۴ ثانیه — پیش‌فرض PTT (فشار-بده-حرف-بزن)
  تجمیعِ کل گفته است، پس تأخیر تجمیع طبیعتِ خود PTT است نه عیب.
- فشرده‌سازی شناسه‌ی اتاق: بعد از ثبتِ اولیه با کلید ۱۶بایتی، فورواردرِ
  ریگ (همان `datagram_relay` روی مک که دو سوکتِ خودِ گوشی را به هم
  هرپین می‌کند) یک تگ ۲بایتی برمی‌گرداند و پکت‌های بعدی فقط تگ می‌برند
  — نسخه‌ی دومِ همان قالب، با تست واحد خودش در server/signaling_server؛
  سازگاری عقب‌رو: پکت با طول‌های قدیمی همان رفتار قدیم.
- گیت دو-سطحی صادق: HARD = کل بایت سیم (پی‌لود+کلید/تگ+UDP/IP محاسبه‌شده)
  در پنجره‌ی ۶۰ثانیه‌ی گفتار پیوسته ≤ 750bps؛ REPORT = آناتومی per-packet
  در TSV. بدون tag-v2 اگر عدد بالای ۷۵۰ ماند، ردیف قرمز می‌ماند — «تقریباً»
  نداریم.

### ب. قله ۴ — کانتینر ممنوع، استریم خام

MP4/MKV/WebM چند کیلوبایت متادیتا دارند و سقف 15KB را می‌سوزانند. الزام:
AV1 خام (IVF-سبک اما stripped) + صدای Codec2، پکیج‌شده با هدر باینری
سفارشیِ ≤ ۱۶ بایت (نسخه/ابعاد/fps/طول). گیت ۴ فقط این فرمت را می‌پذیرد و
اندازه‌ی هدر را جدا در TSV می‌نویسد.

### ج. قله ۱ — بودجه‌ی خود دیکشنری

دیکشنری zstd آموزش‌دیده یک asset کلاینت است: سقف ۳۲KB، کامیت‌شده در
`packages/messaging/assets/zstd_chat.dict`، ورژن‌دار (هش در هدر پیام نه؛
شناسه‌ی ۱بایتی نسخه‌ی دیکشنری در اسکیما). دو تست اجباری: round-trip با
دیکشنری درست، و شکستِ تمیز (نه کرش/دیتای غلط) با دیکشنری نامنطبق.

### د. مرز محیط اجرا — این فاز فقط میزبان (macOS host)

همه‌ی FFI ها (codec2/libavif/svt-av1) در این فاز فقط برای میزبان بیلد و
گیت می‌شوند (dylib/.so + dart test/بنچ روی مک). کراس‌کامپایل iOS
(.framework) و اندروید NDK = فاز ادغام سخت‌افزاری بعدی، صریح در run.json
به‌عنوان non-goal این فاز. گیت‌های بایت‌شمار مستقل از پلتفرم‌اند، پس هیچ
عددی با این تعویق دروغ نمی‌شود — فقط برچسب «measured on host» در TSV
می‌نشیند.

## پیوست هـ — استانداردهای پیاده‌سازی (نسخه‌ی داوری‌شده‌ی «کدهای نسل بعد»)

بازبینی کدهای پیشنهادی بیرونی: چهار ایده پذیرفته، چهار عیب مردود. کد شب
از این قرارداد می‌نویسد، نه از اسنیپت‌ها.

پذیرفته (استاندارد این فاز):
- extension type روی Uint8List برای همه‌ی قالب‌های سیم (view بدون کلاس
  wrapper) — ولی ادعای «صفر تخصیص» ممنوع در کامنت‌ها: خودِ بافر پیام
  همیشه تخصیص است؛ سود واقعی فقط حذف شیء پوششی است.
- FFI با NativeFinalizer برای stateهای codec2/libavif/av1 — درست و لازم.
- هدرهای میکرو: پیام متن 4B = [1B ver/flags][2B msgId][1B dictVer]؛
  ویدیو 12B = [2B magic 0x5631][1B fps][2B w][2B h][4B audioOffset][1B flags].
- بیت-پکینگ فریم Codec2: فریم 700C = ۲۸ بیت، بایت-الاین نیست — تجمیع N
  فریم = ceil(N*28/8) بایتِ پیوسته با شیفت بیتی، نه آرایه‌ی فریم‌های
  ۴بایتیِ پدشده (پد = ۱۲.۵٪ بودجه‌ی سیم دور ریخته).

مردود (اگر در کد شب دیده شود، ریویو رد می‌کند):
- PTT با تجمیع 160ms: ریاضی خودش می‌گوید حین گفتار ~۲۲۰۰bps است و «میانگین
  با سکوت زیر ۷۵۰» یعنی گیت-بازی با duty-cycle. گیت ۵ حین گفتارِ فعال
  می‌سنجد؛ تجمیع مجاز فقط ۱-۴ ثانیه (پیوست الف).
- رندر خبر با drawRect به‌ازای هر پیکسل (۹۶۰۰ فراخوانی در هر paint) و
  پیکسل خاکستری خام 120x80=9600B که به‌تنهایی ۶ برابر کل بودجه‌ی صفحه
  است — تصویر قله‌ی ۶ فقط BlurHash/برداری ≤ 500B، رندر با یک ui.Image
  آپ‌اسکیل‌شده یا drawVertices، نه حلقه‌ی pixel-rect.
- «دی‌ماکسر بدون کتابخانه AV1 را باز می‌کند» — دی‌ماکس ≠ دیکود؛ دیکود
  dav1d/libgav1 می‌خواهد و در گیتِ decode همین صادقانه ثبت می‌شود.
- ادعای GC-pause صدمیلی‌ثانیه‌ای دارت به‌عنوان انگیزه — ساختگی است؛
  انگیزه‌ی واقعی این فاز CPUی کدک است و بهانه‌ی معماری نمی‌شود.

## فاز ۶ — افق معنایی/مولد (فقط پس از سبزیِ کامل فاز ۵؛ ران شبانه واردش نمی‌شود)

داوری پیشنهادهای «فراتر» (RVQ عصبی، تله‌پرزنس لندمارک، کدگذاری لاجیت LLM،
Deep JSCC): دو تای اول و سوم مسیر پژوهشی واقعی‌اند با ادعاهای باد-دار؛
چهارمی روی شبکه‌ی IP اصلاً به آن شکل وجود ندارد. آنچه واقعاً فراتر است و
هیچ‌کدامشان نگفتند، چهار قید مهندسی زیر است:

- R0 «معادل صادقِ Deep JSCC روی IP»: ما PHY و constellation نداریم؛ کانال
  ما پاک‌شدگی پکت است. ترجمه‌ی درستِ «سقوط بدون دیوار»: کدگذاری لایه‌ای/
  پیش‌رونده روی همین لاین RLNC (لایه‌ی پایه‌ی فهم‌پذیری + لایه‌های بهبود،
  هر لایه نسل‌های خودش با سربار هدف متفاوت) — تخریب تدریجی اندازه‌پذیر،
  بدون فیزیک قلابی. این تنها بخش R است که می‌تواند زودتر شروع شود چون
  فقط روی زیرساخت موجود می‌نشیند.
  نقطه‌ی شروع طراحی (UEP — حفاظت نابرابر): L0 پایه با افزونگی هدف ~3x و
  L1 بهبود با ~1.2x؛ رفتار هدف: در اتلاف ۸۰٪ L0 برسد (صدا/تصویر پایه،
  قطعی)، L1 ساقط شود؛ در اتلاف ۱۰٪ هر دو کامل. این دو عدد پیش‌فرضِ
  طراحی‌اند نه ادعا — گیت R0 همین منحنی تخریب را اندازه می‌گیرد و
  افزونگی نهایی از اندازه‌گیری می‌آید.
- R1 صوت RVQ عصبی (هدف صادق: ≤ 1kbps روی سیم؛ ادعای 150-200bps تا
  اندازه‌گیری روی دستگاه فقط «lab claim» ثبت می‌شود).
- R2 پرتره‌ی زنده‌ی لندمارک: به‌عنوان مدیوم جدید («آواتار پرتره») برچسب
  می‌خورد، نه «ویدیو»؛ ادعای 1080p@60 روی NPU موبایل = نامعتبر تا سنجش.
- R3 کدگذاری متن با مدل زبانی مشترک: نظریه درست (مدل مشترک + کدگذاری
  حسابی)، قاتلش تعین‌گرایی است — بدون گیت R-Det هیچ کدی نوشته نمی‌شود.

گیت‌های عرضی که سرنوشت هر چهار R را تعیین می‌کنند (نوآوری واقعی این‌جاست):
- R-Det (تعین‌گرایی): خروجی مدل روی دو محیط مستقل بیت‌به‌بیت یکسان، روی
  کورپوس مرجع — قبل از هر ادعای فشرده‌سازی. شکست = آن R مسدود.
  مکانیزم تجویزی (نه فقط تست): استنتاج فقط-صحیح (fixed-point/int8 با
  کرنل قطعیِ ایزوله)، GPU و FMAی شناور ممنوع در مسیر کدگذاری — یک بیت
  اختلاف در P(next) کافی است تا کدگذاری حسابی از کلمه‌ی دوم به بعد کل
  متن را نابود کند (Apple fp16 و Snapdragon int8 هم‌ارز نیستند). R-Det
  اول قفل می‌شود، بعد حتی یک خط کد فشرده‌سازی نوشته می‌شود.
- R-Ver (قفل نسخه): شناسه‌ی مدل/دیکشنری در هدر + رفتار تعریف‌شده در
  عدم‌تطابق (fallback به مسیر کلاسیک فاز ۵، هرگز خروجی خراب).
- R-Energy: بودجه‌ی انرژی/تأخیر اندازه‌گیری‌شده (mJ به‌ازای پیام، ms
  به‌ازای فریم روی میزبان؛ بعداً دستگاه) در TSV — «هوش روی دستگاه» بدون
  عدد باتری ادعا نیست.
- R-Consent (صدای/چهره‌ی مولد): بازسازی مولد فقط برای هویت خودِ کاربرِ
  رضایت‌داده؛ برچسب «reconstructed» در متادیتای مدیوم اجباری؛ بردار
  timbre/پرتره‌ی شخص ثالث پذیرفته نمی‌شود.

- R4 هم‌راستایی با IP مدرن (راستی‌آزمایی‌شده ۲۰۲۶-۰۸-۱۹): سه سطحِ
  قابل‌طراحی — (۱) دوقلوی QUIC-datagram برای DatagramLanePort
  (RFC 9221؛ رمزنگاری+NAT استاندارد برای مسیر اینترنتِ محصول) با این
  هشدار مکانیکی: CC خود QUIC روی datagram هم اعمال می‌شود و زیر اتلاف
  تصادفی سنگین همان فروپاشی SCTP را بازتولید می‌کند — لاین UDP خام برای
  پروفایل‌های افراطی می‌ماند، دوقلوی QUIC برای اینترنت واقعی که اتلافش
  ازدحامی است؛ (۲) نگاشت لاین‌ها به MoQT (draft-17/18، نزدیک WGLC،
  interop در NAB 2026) — فواره/آبجکت‌ها روی MoQ object با datagram
  preference؛ (۳) علامت‌گذاری NQB/L4S برای پکت‌های PTT (RFC 9330/9331؛
  استقرار dual-queue در ISPها فعال است) — پکت کوچک صوتی به صف کم‌تأخیر.
  سطح غیرقابل‌طراحی برای ما: PHY/constellation (قلمرو 3GPP/6G) — همان
  که R0 جایگزین صادقش است.
  سیاست دو-مسیره (dual-track) صریح: QUIC-datagram = مسیر اینترنت عمومی
  (فایروال/NAT، TLS 1.3، مهاجرت اتصال بین Wi-Fi و سلولی)؛ UDP خام + RLNC
  = مسیر شرایط خصمانه/اختلال شدید، چون پنجره‌ی ازدحام QUIC (CUBIC/BBR)
  با پکت‌لاس تصادفیِ ناشی از فیلترینگ منقبض می‌شود — سوییچ بین دو مسیر
  خودکار و مبتنی بر سنجش (نرخ تحویل مسیر فعال زیر آستانه ← مسیر دیگر
  آزموده می‌شود)، هرگز دستی.

قانون ترتیب: فاز ۵ (کلاسیک، قطعی) جاده‌ی بتنی است و امشب تمام می‌شود؛
R0 اولین قله‌ی فاز ۶ است چون صفر وابستگی به مدل دارد؛ R1-R3 هر یک نشست
پژوهشی خودش با بلوپرینت جدا؛ R4 طراحی‌پذیر است ولی فقط بعد از R0.

## تراک پرونده‌ی حمایت مالی — جای دیگری است

کل آن تراک به `DOSSIER_TRACK.md` منتقل شد: مخاطب و مالکِ تصمیمش فرق
دارد و ران کدک هیچ‌وقت لازم نیست آن را بخواند. این فایل فقط قرارداد
مهندسی است.

## بخش آرم شبانه — یک ران ۰تا۱۰۰ (اپ دسکتاپ، بدون ترمینال)

زیرساخت ضدتوقف مکانیکی است: گاردهای A-D درایور AUTORUN (آرمِ بی‌پلن
بلاک، قدمِ بی‌verify بلاک، زنجیره‌ی مسلح‌نشده قدم را نمی‌بندد، پایان
فقط با goal_verify سبز). سلامت پیش از آرم: هر دو سوئیت autorun سبز.

یک نشست، همین یک پیام:

```
AUTORUN: فاز ۵ طبق PLAN_PHASE5_CODEC.md — کل فایل را با پیوست‌های الف تا هـ بخوان؛ قرارداد باینری همان‌هاست. نوبت اول: tools/phase5/ را بساز (کورپوس ثابت، gate_1..6.sh بایت‌شمار با سطر TSV در tools/phase5/h3_results.tsv، goal_verify.sh) و .autorun/run.json را بنویس: شش قله به ترتیب ۱←۶←۲←۳←۵←۴، هر قله سه قدم build→gate→tsv-row، هر قدم verify_cmd فقط-چک‌کننده‌ی exit-0، arm_next تنها روی قدم‌های با کار طولانی و هرگز روی قدم آخر، goal_verify_cmd سطح ران. عدد بدون منبع TSV/لاگ ممنوع؛ fallback فقط با برچسب خودش؛ FFI فقط برای میزبان با برچسب measured-on-host؛ قله‌ی ناتمام = REPORT.md با خطای verbatim، نه ادعای سبز. تا صبح هیچ سؤالی نپرس.
APPROVE: brew-install,pub-add
```

تراک پرونده در نشست جدا و با ارجاع به `DOSSIER_TRACK.md` آرم می‌شود —
هرگز در همان نشست کدک (دو ران در یک پوشه سرِ یک run.json می‌جنگند).

## پیوست و — قراردادهای مسیر و نام (بسته‌شده پس از تست خشکِ فابل ۵، ۲۰۲۶-۰۸-۱۹)

یک اجرای آزمایشیِ نوبتِ اول نشان داد پنج چیز بازند و ران شبانه ناچار
می‌شود قرارداد اختراع کند — که یعنی گیت و goal_verify سرِ نام فایل به هم
نمی‌رسند. این‌ها از حالا ثابت‌اند:

```
tools/phase5/corpus/        assets مرجع + manifest.tsv با sha256 هر قلم
tools/phase5/native/        dylib های میزبان (staged)
tools/phase5/.markers/      مارکر پایانِ بیلدهای طولانی
tools/phase5/logs/          لاگ کاملِ هر گیت (هرگز tail/head)
tools/phase5/artifacts/     مصنوع شنیدنی/دیدنی برای قضاوت مالک
tools/phase5/h3_results.tsv جدول رسمی
```

- قدم صفر (قبل از قله ۱): ساخت کورپوس مرجع با نام‌های ثابت —
  `chat_corpus_200.jsonl` · `photo_ref.jpg` · `speech_ref_10s.wav` ·
  `clip_ref_5s.mov` · `news_ref_page.json` — و ثبت sha256 هرکدام در
  `manifest.tsv`. هیچ گیتی روی asset بی‌هش اجرا نمی‌شود (عددی که ورودی‌اش
  قابل‌بازتولید نیست، عدد نیست).
- نام ردیف‌های TSV دقیقاً: `Text` · `NewsPage` · `Photo` · `VoiceNote` ·
  `PTT` · `VideoNote`. ستون Status فقط سه شکل: `PASS` یا
  `PASS(fallback:<نام دقیق>)` یا `FAIL`.
- فایل دارتِ هر کدک در همان پکیجی می‌نشیند که مالک آن مدیوم است و نامش
  در desc همان قدم نوشته می‌شود؛ goal_verify فقط به `tools/phase5/` و
  TSV تکیه می‌کند تا به چیدمان پکیج‌ها گره نخورد.
- معیار مکانیکیِ «بقا زیر ۶۰٪ اتلاف» در گیت ۵ (تا امروز تعریف‌نشده بود):
  استریم ۶۰ثانیه‌ای نباید قطع شود، نسبت فریم‌های decode شده باید در
  بازه‌ی ±۵٪ از ۴۰٪ نظری بماند (PTT بدون FEC است، پس ۴۰٪ درست است نه
  عیب)، و تأخیرِ صف نباید رشد بی‌کران کند. کیفیت ادراکی مکانیکی نیست →
  artifact برای مالک.
- معنای `arm_next` (تا امروز حدس بود): خروجیِ صفر یعنی «ادامه مسلح است»
  — یا پروسه‌ی پس‌زمینه زنده است یا مارکر پایانش موجود. الگوی مجاز:
  `pgrep -f '<cmd>' >/dev/null || test -f tools/phase5/.markers/<x>.done`
  فقط-خواندنی و idempotent؛ خودش هرگز کاری را استارت نمی‌زند (درایور در
  هر سیکل Stop دوباره اجرایش می‌کند).

## مرز صداقت برای گزارش صبح

هر قله یا «PASS با سطر TSV و artifact» است یا «باز، با آخرین خطای دقیق در
REPORT.md» — هیچ حالت سومی مجاز نیست. قله‌ی ناتمام شکست نیست؛ گزارشِ
دروغ هست.
