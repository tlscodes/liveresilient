{
  "name": "voice-call-relay-worker",
  "private": true,
  "type": "module",
  "description": "Border relay worker: call lanes plus the broadcast archive routes.",
  "scripts": {
    "test": "node --test test/"
  }
}
