# REPORT — ران FULL_TEST (T0→T5) — همه‌چیز سبزِ مدرک‌شده: ۷/۷ گیت + فاز ۵

به‌روزرسانی نهایی 2026-08-19 22:14Z. بازتأیید سطح ران، عین خروجی:

```
goal_verify_full 2026-08-19T22:14:33Z
  gate_t0_device: GREEN     gate_t3_e2e: GREEN
  gate_t1_tree: GREEN       gate_t4_uplift: GREEN
  gate_t1b_probe: GREEN     gate_t5_docs: GREEN
  gate_t2_ios: GREEN        phase5_goal_verify: GREEN
FULL GOAL VERIFY GREEN: 7/7 track gates + phase-5 goal_verify (exit=0)
```

## جدول رسمی ماتریس دستگاه (کپی عینی از tools/dossier/e2e_ios_results.tsv)

روی آیفون واقعی، زیر پروفایل سختِ اندازه‌گیری‌شده (rtt مشاهده‌شده 2263ms،
اتلاف سرجمع ۶۰٪، پهنای 2Kbps بر عبور) — هر پی‌لود دقیقاً بایت‌های فاز ۵،
هر دیکد با کدک نیتیوِ خودِ دستگاه:

```
Feature     Wire      Budget_s  Measured_s  Status  Note
chat        29        4.8       2.5         PASS    decoded=verified
news        1160      52.3      17.0        PASS    page=verified
photo       2682      49.6      23.2        PASS    120x160,render=pumped
voice_note  879       42.3      13.9        PASS    frames=250,decode=complete
video_note  5926      82.0      45.4        PASS    frames=15/15,128x96
ptt         5220/60s  live      60.0        PASS    ratio=0.167,err=none
```

## بودجه‌ها: از جدول ثابت به کرانِ علمی

جدول ثابت اول، جمله‌ی اتلاف نداشت — اجرای اول همین را عریان کرد: پنج
ردیفِ از نظر کارکرد بی‌نقص، از بودجه رد شدند (photo 74.1s وقتی خودِ
جمله‌ی انتقالِ فرمول 26.8s است؛ بودجه‌ی 25s از نظر فیزیکی ناممکن بود).
طبق حکم خود پلن («بودجه‌ها از نو مشتق می‌شوند نه کپی»)، کرانِ کانالِ
تصادفی با اطمینان ۹۹٪ ابزار شد — همه‌ی ورودی‌ها منبع‌دار، RTT از
اندازه‌گیری:

```
T99 = S*8/(B*(1-L)) + N99*pace + RTT      N99 = ceil(ln(1-P)/ln(L)) = 10
tools/dossier/stochastic_sla.py -> derived_budgets.tsv
```

سپس انتقال تا نزدیکی کران تیون شد (سیمبل فواره 1024→512، کف نرخ 250→375
تا حفره‌های ارسال پر شود) و نتیجه‌ی اندازه‌گیری‌شده:

```
photo  74.1s -> 23.2s   (3.2x بهتر؛ حتی زیر بودجه‌ی قدیمی 25s)
video  97.2s -> 45.4s   (2.1x بهتر)
```

داوریِ پیشنهاد بیرونی: فرمول کرانش درست بود و پذیرفته شد؛ ادعای «عکس
18-22 ثانیه»اش با فرمولِ خودش ابطال شد (جمله‌ی انتقال به‌تنهایی 26.8s)؛
پکت 64 بایتی زیر سربار 30B هدر یعنی ۴۷٪ اتلاف سربار — رد. رندر لایه‌ای
(BlurHash-first) درست است و زیرساختش موجود؛ به‌عنوان تعهد طراحی UI ثبت
شد نه ادعای امشب.

## تراک به تراک (بی‌تغییر از نیم‌روز، همه سبز)

- T0 دستگاه READY؛ T1 درخت ۷۴/۷۴ + بستنِ ریشه‌ایِ flake رله (AOT-cache)؛
  T1b گیت‌های probe_defense صفر/صفر؛ T2 هفت فریم‌ورک نیتیو پین‌شده +
  FFI بدون‌کرش + بوت Release روی دستگاه؛ T4 چهار ارتقای ثبت‌شده در
  دفترچه (از جمله گاردی که همان لحظه باگ واقعی I444 را گرفت)؛ T5
  مانیفست ۲۴ قلم هش‌دار + سه سند + لینت عدد-منبع صفر.

## درس‌های سوخته‌ی امروز (در حافظه هم ثبت است)

- تونل دیباگ CoreDevice زیر اتلاف سنگین UDP نمی‌ماند → الگوی
  اتصال-سپس-شیپ با سدِ rtt-پروب (خودش مدرک عبور از مسیر شیپ‌شده).
- گوشیِ قفل = خطای Locked در لانچ؛ دیدبان lockState تا مهلت فعال بود و
  اجرای نهایی با حضور مالک انجام شد.
- bash پیش‌فرض مک declare -A ندارد؛ گیت‌ها فقط قابل‌حمل نوشته شوند.
- نام کانتینر XCFramework = نام فریم‌ورک داخلی، وگرنه لینک می‌شکند.

## کارهای [مالک]

- قضاوت گوش: سه آرتیفکت صوتی فاز ۵ (700C / 450 / VBR).
- ویدیوی بدون‌کات (T3.4): سمت مک آماده — tools/dossier/owner_video_rig.sh
- کامیت git با staging فایل‌به‌فایل (درخت فایل نامرتبط دارد).

## پس‌نوشت پاس ارتقای دوم (2026-08-20، به دستور مالک)

سه «ای کاش» باقی‌مانده به نسل بعد رفت و مکانیکی اثبات شد (دفترچه: هفت
ردیف):

- نشتی خاموش بافرها در codec2_ffi بسته شد (finalizer مستقل هر بافر).
- ZstdChat به CDict/DDict هضم‌شده + کانتکست پایدار ارتقا یافت.
- خطای مبهم انکودر brotli روی دستگاه فقط-دیکد حالا خودش را می‌گوید.

بازتأیید پس از پاس: ران کامل ۷۴/۷۴ سبز و
FULL GOAL VERIFY GREEN 7/7 + phase5 (22:59Z). یک فلیک زمانی در تست
خود probe_defense دیده شد (بودجه‌ی 2ms زیر بار suite شد 2.69ms؛ روی
ماشین آرام و در ران کامل بعدی سبز) — دست‌نخورده و همین‌جا ثبت شد.
