# پلن «قطره‌ی آب در صحرا» — ارتباط صوتی پایدارِ بدون قطعی، رکوردشکن

قاعده‌ی همه‌ی فازها: هیچ فازی بسته نمی‌شود مگر همه‌ی آیتم‌های چک‌لیستش با خروجی ابزار (exit 0 / عدد چاپ‌شده) سبز باشد؛ عدد بدون خروجی ابزار = ادعای باطل. هر فاز یک کامیت مستقل.

---

## فاز ۰ — ساخت کتابخانه‌ی مرجع v4 + تست روی صدای کاربر

هدف: کدک v4 از «اسکریپت آزمایش» به «کتابخانه‌ی مرجع تمیز» تبدیل شود — یک ماژول با API روشن (state، encode/decode، ماندگاری JSON، سشن ack-دار) و مجموعه‌تست که مستقیم روی فایل صدای واقعی کاربر اجرا می‌شود.

کارها:
- `tools/hamseda_v4.py`: کتابخانه‌ی مرجع (بدون کد آزمایشی؛ فقط API).
- `tools/test_hamseda_v4.py`: مجموعه‌تست اجراشونده با یک فرمان روی `/tmp/gift_tokens.json` (توکن صدای کاربر).

چک‌لیست تأیید:
- [ ] roundtrip بیت‌به‌بیت سرد روی توکن‌های صدای کاربر (assert).
- [ ] ماندگاری JSON: state ذخیره/بارگذاری، تماس دوم بیت‌به‌بیت و کوچک‌تر.
- [ ] سشن ack-دار: loss صفر/۵٪/۲۰٪، صفر واگرایی state.
- [ ] اعداد رکورد بازتولید شوند: warm full-band < 100bps و row0 fresh < 700bps.
- [ ] همه‌ی تست‌ها با exit 0 در یک فرمان؛ کامیت با اعداد.

---

## فاز ۱ — v4 داخل محصول (پورت دارت مدل مرتبه‌۱ ستونی)

هدف: عدد رکورد از اسکریپت Python به پکیج `hamseda_codec` منتقل شود تا رانگ tokenVoice واقعاً همین کدک را روی سیم بزند.

کارها:
- افزودن جدول‌های زمینه‌ی ستونی (prev-column-id → FreqTable) به `HamsedaState` + مسیر escape سه‌پله‌ای (زمینه → سراسری → ردیف‌ها).
- سریال‌سازی JSON زمینه‌ها (ماندگاری بین‌تماسی) + clone برای سشن ack-دار.
- ابزار برابری بین-زبانی: dump توکن از Python، انکود در دارت، مقایسه‌ی بایت‌به‌بایت یا حداقل برابری طول+دیکود.

چک‌لیست تأیید:
- [ ] `dart analyze --fatal-infos` روی پکیج: صفر issue.
- [ ] تست roundtrip بیت‌به‌بیت v4 (سرد) سبز.
- [ ] تست ماندگاری: state ذخیره/بارگذاری‌شده، تماس دوم بیت‌به‌بیت + کوچک‌تر از تماس اول.
- [ ] تست loss صفر/۵٪/۲۰٪ با سشن ack-دار: صفر واگرایی state (مقایسه‌ی JSON دو سر).
- [ ] تست مرز: arity غلط، ورودی خراب، عبور از آستانه‌ی halving در جریان طولانی.
- [ ] برابری بین-زبانی روی `/tmp/gift_tokens.json`: عدد بیت دارت == Python (تلورانس صفر).
- [ ] `bash tools/run_gate_loop.sh` → `GATE LOOP OK`.
- [ ] کامیت با اعداد در پیام.

---

## فاز ۲ — صادق‌سازی رکورد (پیکره‌ی چند-جلسه‌ای)

STATUS ۲۰۲۶-۰۷-۲۵: ابزار `tools/corpus_curve.py` ساخته و سبز (جلسات ماندگار: 1501 → 84 → 78bps، همه بیت‌به‌بیت). بلاکر خارجی: ≥۵ دقیقه ضبط جدید از صدای کاربر لازم است — اسلات: جلسه‌ی بعدی کاربر ضبط‌ها را می‌دهد، سپس سنجه‌ی رسمی fresh|history اجرا و سند به‌روز می‌شود.

هدف: عدد رسمی از «warm روی گفتار دیده‌شده» به «تماس تازه پس از تاریخچه» ارتقا یابد — عددی که بشود جهانی دفاع کرد.

کارها:
- پیکره: حداقل ۵ ضبط جدید از صدای کاربر (مجموع ≥ ۵ دقیقه؛ محتوای متفاوت، روزهای متفاوت اگر شد).
- ابزار `corpus_curve.py`: تماس‌ها به‌ترتیب، state ماندگار، خروجی جدول bps هر تماس + منحنی.
- تعریف سنجه‌ی رسمی: `bps(fresh call | N دقیقه تاریخ)` برای N=1,3,5.

چک‌لیست تأیید:
- [ ] هر تماس جدید بیت‌به‌بیت دیکود شود (assert در ابزار).
- [ ] جدول کامل bps چاپ‌شده از ابزار (نه دستی) در سند رکورد.
- [ ] منحنی نزولی: تماس آخر < تماس اول (عدد دقیق ثبت).
- [ ] سنجه‌ی رسمی زیر 700 در یکی از دو لاین (full یا row0) → اگر نشد، عدد واقعی با برچسب صادق ثبت و دلیل تحلیل شود.
- [ ] فایل شنیداری هر دو لاین کنار سند.
- [ ] به‌روزرسانی `docs/RECORD_hamseda_token_voice.md` + SVG با اعداد جدید.
- [ ] کامیت.

---

## فاز ۳ — نردبان بی‌قطعی تا آخرین قطره

هدف: صدا هرگز «قطع» نشود؛ فقط لاغر شود — زنجیره‌ی کامل رانگ‌ها با بودجه‌ی بیت مشخص و سوییچ خودکار دوطرفه.

کارها:
- تعریف بودجه‌ی هر رانگ در یک جدول کد (نه پراکنده): full → reduced → audioOnly → lowRate(6k) → tokenVoice-full(~1.5k خام) → tokenVoice-row0(750 خام) → voiceNotes → text.
- سیم‌کشی زیر-رانگ row0 در tokenVoice (انتخاب بر اساس bps مجاز گزارش‌شده از فابریک).
- سناریوی «پهنای باند در حال مرگ»: شبیه‌ساز که ظرفیت را پله‌پله از ۵۰۰kbps به ۲۰۰bps می‌کشد و بعد برمی‌گرداند.

چک‌لیست تأیید:
- [ ] تست: در کل سناریوی مرگ تدریجی، هیچ لحظه‌ای بدون رانگ فعال نیست (invariant: always-some-mode).
- [ ] تست: هر گذار فقط یک پله است (بدون پرش دوتایی ناگهانی به جز قطع کامل).
- [ ] تست: بازگشت ظرفیت → صعود خودکار تا full (هیسترزیس، بدون فلپ؛ شمارش سوییچ‌ها زیر آستانه).
- [ ] تست: در ۲۰۰bps رانگ tokenVoice-row0 یا voiceNotes فعال و بسته‌ها ack می‌شوند.
- [ ] `flutter analyze --fatal-infos` صفر + کل گیت `GATE LOOP OK`.
- [ ] کامیت.

---

## فاز ۴ — قطره‌جمع‌کن (تماس نیمه‌زنده روی لینک منقطع)

هدف: روی لینکی که فقط گاهی چند ثانیه نفس می‌کشد، مکالمه‌ی توکنی ادامه یابد — بلوک‌های ack-دار hamseda روی store-and-forward موجود.

کارها:
- آداپتور بلوک توکنی → DtnBundle (اولویت، عمر، ترتیب) + مسیر برگشت ack.
- بازپخش سمت گیرنده به‌ترتیب با vector ساده‌ی شماره‌ی بلوک (پیش‌درآمد آیتم causal-order بک‌لاگ).
- سناریوی شبیه‌سازی «لینک نبض‌دار»: ۳ ثانیه وصل، ۳۰ ثانیه قطع، تصادفی با seed ثابت.

چک‌لیست تأیید:
- [ ] تست: در سناریوی نبض‌دار، ۱۰۰٪ بلوک‌های تولیدشده در پنجره‌ی عمر تحویل و بیت‌به‌بیت دیکود می‌شوند.
- [ ] تست: ترتیب بازپخش == ترتیب تولید (هیچ جابه‌جایی).
- [ ] تست: قطع وسط بلوک → rollback فرستنده، صفر واگرایی state (مقایسه‌ی JSON).
- [ ] تست: عمر تمام‌شده → حذف تمیز + شمارنده‌ی گزارش، بدون crash.
- [ ] گیت کامل سبز + کامیت.

---

## فاز ۵ — پیش‌بینِ زبانی (حمله به مرز ۵۰)

هدف: سنجش این ادعا که پیش‌بینِ زبانی توکن صوتی، آنتروپی را از سطح صدا به سطح زبان می‌برد.

کارها:
- محک Python فقط: مدل کوچک (همان qwen3:0.6b بیک‌آف یا n-gram روی توکن‌ها) به‌عنوان prior مخلوط با مدل v4؛ اندازه‌گیری Δbps روی پیکره‌ی فاز ۲.
- تصمیم‌نامه: اگر بهبود ≥ ۱۵٪ → پلن پورت؛ اگر نه → ثبت صادقانه و توقف این خط.

چک‌لیست تأیید:
- [ ] عدد Δbps از ابزار با بیت‌به‌بیت assert.
- [ ] مقایسه‌ی منصفانه: همان پیکره، همان پروتکل فاز ۲.
- [ ] سند تصمیم (برو/نرو) با اعداد + کامیت.
- [ ] حافظه‌ی پروژه به‌روز.

---

## بستن کلی (پس از فاز ۵)

- [ ] `GATE LOOP OK` نهایی روی کل مونوریپو.
- [ ] سند رکورد نهایی: سه عدد رسمی (warm / fresh-with-history / زیر-لاین row0) + برچسب‌های صادق + پروتکل تکرار.
- [ ] SVG و سه نمودار به‌روز و کامیت.
- [ ] گزارش صبح: چه شد، چه ماند، اعداد.
- [ ] بک‌لاگ باز (MOS انسانی، تست دو-دستگاهه، کدک اختصاصی Colab) با تاریخ ثبت.
