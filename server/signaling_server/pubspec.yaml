name: signaling_server
description: >-
  Minimal wss:// signaling relay: pairs sockets by callId and forwards
  opaque frames between them, no payload parsing or trust.
version: 2.0.0
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dev_dependencies:
  test: ^1.25.0
